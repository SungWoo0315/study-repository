# 2021년 7월 28일 [53일차]

## # 오전수업
----
### `09:30~`

<복습>

#### # 어제 이어서. `append( )` 메서드 문제. 복습.  

- 테스트 파일.

  [`append_test.html`](https://github.com/SungWoo0315/new-study-repository/blob/main/JQuery/html/append_test.html) 파일 참고.  

#

[JQuery_교재.txt](https://github.com/SungWoo0315/new-study-repository/blob/main/JQuery/JQuery_%EA%B5%90%EC%9E%AC.txt)  
`append( "html코드" )`, `prepend( "html코드" )` 부분 참고.  
정답과 오답이 교재에 있다.  

#

- 문제에서 아래 이미지와 같이 `<div>존윅3</div>` 이 들어가야 한다.

  ![문제.원하는 위치](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210728%200949_append_%EB%AC%B8%EC%A0%9C_.png)    


- `contains('데드풀')` 을 사용한 오답.

  ![contains 설명](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210728%201000_contains_%EB%AC%B8%EC%A0%9C_.png)  

  > contains 를 사용하면 div 태그가 2개 잡히게 된다.

  > movie 태그의 후손 div 태그 중에, 
  > `데드풀` 을 내포하고 있는 태그는 부모 태그 까지 2개이다.  


  ![contains+parent 설명](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210728%201003_contains_parent_%EB%AC%B8%EC%A0%9C_.png)  

  > 위의 오답에서 `parent( )` 를 사용하여 `append( )` 를 적용해도 
  > 부모 태그가 2개 잡혀서 이미지와 같이 적용된다.

----
### `10:30~`

#### # `next( )` 는 본인 다음에 나오는 `동생` 이다.  후손이 아니다. 주의!

![`next( )`설명](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210728%201039_next_%EB%A9%94%EC%84%9C%EB%93%9C_%EC%A3%BC%EC%9D%98_.png)  

#

`remove( )`, `empty( )` 주의 하기.  

#

## # JQuery_교재 이어서 진도. 

`attr( '태그속성명' )`  ★★★★★

```
********************************************
attr( '태그속성명' )  ★★★★★
********************************************
    현재 JQuery 객체가 관리하는 태그에 [태그속성명] 에 해당하는 [속성값]을 [리턴]한다.
    --------------------------------------------------------
    <예>
    --------------------------------------------------------
        <input type="text" name="uid" class="uid">
        ----------------------------------------------------
        var str = $(".uid").attr( "name" );
        ----------------------------------------------------
            class="uid" 를 가진 태그의 name 속성값을 리턴해서 str 변수에 저장하라.  
```
> 사용빈도가 높다.  

> 속성명에 해당하는 속성값을 얻어올때 사용한다.  

#


`attr( "태그속성명", "태그속성값" )`  ★★★★★

```
********************************************
attr( "태그속성명", "태그속성값" )  ★★★★★
********************************************
    현재 JQuery 객체가 관리하는 태그에 [태그속성명] 에 [태그속성값]을 [삽입]한다.
    <주의> 태그에 속성 값이 수정되므로 태그의 특징이 바뀌는 것과 동일하므로, 조심히 사용해야 한다. 
    --------------------------------------------------------
    <예>
    --------------------------------------------------------
        <input type="text" name="uid" class="uid">
        ----------------------------------------------------
        $(".uid").attr( "name", "user_id" );
        ----------------------------------------------------
            class="uid" 를 가진 태그의 name 속성값을 "user_id" 로 수정하라.  
```
> 조심해서 써야한다.

> 태그 속성값이 바뀌면 특징이 바뀌어서 오류가 날 수 있다.

> name 값을 함부로 바꾸면 안된다!! DB 연동할때 잘못 될 수가 있다.  

#

`attr( {"태그속성명":"태그속성값", "태그속성명":"태그속성값", ~} )`  ★★★★★

```
********************************************
attr( {"태그속성명":"태그속성값", "태그속성명":"태그속성값", ~} )  ★★★★★
********************************************
    현재 JQuery 객체가 관리하는 태그에 [여러개의] [태그속성명] 에 [태그속성값]을 [삽입]한다.
```
> JSON 형태로 주면 여러개의 속성값을 바꿀 수 있다.  

> JSON 형태가 기억 안나면 attr() 메서드를 연달아 쓰면 된다.  

> 키값은 저장목적이 아니라, 키값이 붙어있는 데이터를 찾을 때 사용한다.  

#

`removeAttr( "태그속성명" )`  

```
********************************************
removeAttr( "태그속성명" ) 
********************************************
    현재 JQuery 객체가 관리하는 태그에 [태그속성명] 에 [태그속성값]을 [삭제]한다.
    --------------------------------------------------------
    <예>
    --------------------------------------------------------
        <input type="text" name="uid" class="uid" size=100>
        ----------------------------------------------------
        $(".uid").removeAttr( "size" );
        ----------------------------------------------------
            class="uid" 를 가진 태그의 size 속성명과 속성값을 삭제한다. 
            결론적으로  
            <input type="text" name="uid" class="uid" size=100>
            는
            <input type="text" name="uid" class="uid">
            로 바뀐다.  
```
> 특정 `속성명`과 `속성값` 모두가 삭제된다.  

#

- 4가지 메서드는 

  태그 자체에 들어있는 속성값을 관리한다. 

  `attr( '태그속성명' )`

  `attr( "태그속성명", "태그속성값" )`  ★★★★★

  `attr( {"태그속성명":"태그속성값", "태그속성명":"태그속성값", ~} )`  ★★★★★

  `removeAttr( "태그속성명" )`  

----
### `11:30~`


`prop( "DOM객체의 속성변수명" )`   

```
********************************************
prop( "DOM객체의 속성변수명" ) 
********************************************
    현재 JQuery 객체가 관리하는 태그를 웹브라우저가 읽어들여 DOM 객체로 생성할 때,
    이 [DOM 객체]의 [속성변수]의 [값]을 [리턴]한다.
```
>

#

`prop( "DOM객체의 속성변수명", "데이터" )` 

```
********************************************
prop( "DOM객체의 속성변수명", "데이터" ) 
********************************************
    현재 JQuery 객체가 관리하는 태그를 웹브라우저가 읽어들여 DOM 객체로 생성할 때,
    이 [DOM 객체]의 [속성변수]에 [데이터]를 [저장]한다.
```
>

#

```
attr 은 태그의 접근.  

prop 은 DOM 객체의 접근.    

오후에 그림으로 prop 설명.  
```

#

`each(  function(index){실행구문;}  )`  ★★★★★★★★★★

```
********************************************
each(  function(index){실행구문;}  )  ★★★★★★★★★★
********************************************
    현재 JQuery 객체가 관리하는 태그들에 차례대로 접근해서 실행구문을 반복 실행한다.
    <비유> 학생 붙잡고, 청소해!
    --------------------------------------------------------
    <예>
    --------------------------------------------------------
        <input type="text" name="mem_id" class="mem_id">
        <input type="text" name="mem_name" class="mem_name">
        <select name=addr class="addr">
            <option value=""></option>
            <option value="서울">서울</option>
            <option value="경기">경기</option>
            <option value="인천">인천</option>
            <option value="기타">기타</option>
        </select>
    --------------------------------------------------------
        $(".mem_id,.mem_name,.addr").each(
            function(i){
                var value = $(this).val();
                var name = $(this).attr("name");
                
                if( value.split(" ").join("")=="" ){
                    alert( name + " 는(은) 데이터가 비었습니다. 입력 또는 선택해 주세요.")
                }
            }
        );
    --------------------------------------------------------
        class="mem_id", class="mem_name", class="addr" 를 가진 태그를 관리하는 JQuery 객체를 생성하고,
        each 메서드를 호출하여    function(i){ 실행구문 }    익명함수의 실행구문을 실행한다.   
```
> 중요! 프로젝트 때 자주 사용한다.   

> 일종의 `반복문`과 흡사하다.    

> 익명함수 사용.  

> 새로 배울 선택자 `this`  
> `this` 는  현재 `each( )` 가 반복하면서 잡은 i번째 태그를 지칭할 때 사용.

> `this` 앞뒤에는 `""`, `''` 더블쿼트, 싱글쿼트 가 오지 않는다.  주의하기!  

> `checkbox` 나 `radio` 는 체크된 개수를 판단하여 유효성 검사를 해야 한다.  

----
### `12:30~`

### <<<언어별로 `this`>>>

- 자바의 this  

- 자바스크립트의 this    
  생성자 함수에서 사용.  

- JQuery 의 this    
  each 메서드를 사용하여 i번째 태그를 지칭할 때 사용.  

#

### <<<언어별로 `''`, `""` 쓰임새.>>>

- java `''` char 형

- java `""` String 형

- 오라클에서는 컬럼명 및 별칭 `""`

- 자스에서는 문자열 감쌀 때 `""`, `''` 둘다 사용가능. 원칙은 `""` 이다.  

----
### `13:20~`

  - 점심시간.

---
---

## # 오후수업

---
### `14:30~`

- `prop` 메모리 상상도 

  ![](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210728%201501_JQuery_prop_.png)    

  > 태그안에는 checked = true 가 들어가면 안된다. DOM 객체로 접근해서 checked 접근 해야한다.  

  prop 을 써야 할 경우, 체크 집어넣거나 체크 뺄때, attr 말고 prop 으로 접근해야 한다. boolean 값 true false 때문에.

  `checkbox` 나 `radio` 입력양식은 `prop` 을 써서 DOM 객체 접근하여 checked 속성변수에 접근해야 한다. attr로는 안됨.   

#

`prop( "DOM객체의 속성변수명", "데이터" )`   

```
********************************************
prop( "DOM객체의 속성변수명" ) 
********************************************
    현재 JQuery 객체가 관리하는 태그를 웹브라우저가 읽어들여 DOM 객체로 생성할 때,
    이 [DOM 객체]의 [속성변수]의 [값]을 [리턴]한다.
    --------------------------------------------------------
    <참고> 주로 checkbox 입력양식 또는 radio 입력양식에 체크여부를 얻을 때 사용한다.  
    --------------------------------------------------------
        --------------------------------------------------------
        <input type="checkbox" name="skill" value="JSP" class="skill">JSP
        <input type="checkbox" name="skill" value="ASP" class="skill">ASP
        <input type="checkbox" name="skill" value="PHP" class="skill">PHP
        --------------------------------------------------------
        <문제> ASP 의 체크여부를 얻으려면?
        --------------------------------------------------------
            var flag = $(".skill)").filter("[value=ASP]").prop( "checked" ) 

********************************************
prop( "DOM객체의 속성변수명", "데이터" ) 
********************************************
    현재 JQuery 객체가 관리하는 태그를 웹브라우저가 읽어들여 DOM 객체로 생성할 때,
    이 [DOM 객체]의 [속성변수]에 [데이터]를 [저장]한다.
    --------------------------------------------------------
    <예> checkbox 입력양식 또는 radio 입력양식에 체크를 해주거나 체크를 뺄때 많이 사용한다.  
    --------------------------------------------------------
        <input type="checkbox" name="skill" value="JSP" class="skill">JSP
        <input type="checkbox" name="skill" value="ASP" class="skill">ASP
        <input type="checkbox" name="skill" value="PHP" class="skill">PHP
        --------------------------------------------------------
        <문제> 코딩으로 JSP 를 체크하려면?
        --------------------------------------------------------
            $(".skill").eq(0).attr( "checked", true )   (X)

            $(".skill").eq(0).prop( "checked", true )   (O)
            $(".skill:eq(0)").prop( "checked", true )   (O)
            
            // 가장 정확함. 순서상관없이 찾아들어갈 수 있다. ★★★★★★★★★★
            $(".skill)").filter("[value=JSP]").prop( "checked", true )   (O) 
        --------------------------------------------------------
```
> 태그의 속성명 찾으러 들어갈 땐 `[]` 대괄호 들어가야 한다.

> 체크 여부와 상관없는것들은 `attr` 사용하면 된다.

> 체크 여부 관련은 `prop` 을 써야 한다.

---
### `15:30~`

[`jquery_test_01.html`](https://github.com/SungWoo0315/new-study-repository/blob/main/JQuery/html/jquery_test_01.html) 파일. 실습.  

```javascript

//-------------------------------------------
//$(document).ready(   function(){자스코딩}  );
//-------------------------------------------
// body 태그 안의 요소들을 읽어들인 후 익명함수 내부의 자스코딩을 실행.
$(document).ready(function() {
  
  // return;	// 코딩 실행을 멈추고 싶을 때 return 활용.  
  // ---------------------------------------------------
  // id="staff" 가 있는 태그의 후손 중, <thead> 태그 안의 후손 중 tr 태그의 
  // 배경색을 gray 색상으로 설정하기.
  // ---------------------------------------------------
  var tableObj = $("#staff");
  tableObj.find("thead tr").attr("bgcolor", "gray")
  // ---------------------------------------------------
  // id="staff" 가 있는 태그 후손의 <tbody> 후손의 tr 중
  // 인덱스번호가 홀수(사람입장에선 짝수) 인 놈에 배경색을 lightgray 로 바꿈.
  // ---------------------------------------------------
  tableObj.find("tbody tr:odd").attr("bgcolor", "lightgray")
  // ---------------------------------------------------
  // id="staff" 가 있는 태그 후손의 <tbody> 후손의 tr 중
  // 인덱스번호가 짝수(사람입장에선 홀수) 인 놈에 배경색을 #FFFFFF 로 바꿈.
  // ---------------------------------------------------
  tableObj.find("tbody tr:even").attr("bgcolor", "#FFFFFF")


  // alert( tableObj.length );  // 태그가 잡혔는지 확인.


  tableObj.find("tbody tr:eq(0)").mouseover(
    function(){
      // alert("첫번째 tr 에 마우스 온! ");
      $(this).attr("bgcolor", "lightyellow")
    });

    tableObj.find("tbody tr:eq(0)").mouseout(
    function(){
      // alert("첫번째 tr 에 마우스 온! ");
      $(this).attr("bgcolor", "#FFFFFF")
    });

});

```

> 익명함수 안의 코딩. 실행 안되게 하려면?   

> 주석처리 말고 `return;` 을 넣어주면 된다.  



> 마우스 대는 이벤트 `mouseover`  

> 마우스 떼는 이벤트 `mouseout`  


----
### `16:30~`

- 마우스 갖다대고 뺐을때 각각 색상변경.

  ```javascript

  // 인덱스 짝수 의 마우스 갖다댓을 때 색상변경
  tableObj.find("tbody tr:even").mouseover(
    function(){
      // alert("첫번째 tr 에 마우스 온! ");
      $(this).attr("bgcolor", "pink")
    });

  // 인덱스 짝수 의 마우스 뺐을 때 색상변경
  tableObj.find("tbody tr:even").mouseout(
    function(){
      $(this).attr("bgcolor", "#FFFFFF")
    });
  


  // 인덱스 홀수 의 마우스 갖다댓을 때 색상변경
  tableObj.find("tbody tr:odd").mouseover(
    function(){
      // alert("첫번째 tr 에 마우스 온! ");
      $(this).attr("bgcolor", "pink")
    });

  // 인덱스 홀수 의 마우스 뺐을 때 색상변경
  tableObj.find("tbody tr:odd").mouseout(
    function(){
      $(this).attr("bgcolor", "lightgray")
    });
  ```

#

`hover( )` 

```javascript
  tableObj.find("tbody tr").hover(
    function(){
      // $(this).attr("bgcolor", "pink")
      $(".msg").html("<div>마우스 댔니?</div>")
    }
    ,function(){
      // $(this).attr("bgcolor", "lightgray")
      $(".msg").html("<div>마우스 뻈니?</div>")

    }	
  );
```
> `hover( )` 를 사용하여 `mouseover` 과 `mouseout` 을 한꺼번에 구현.  
> 익명함수 두개를 사용한다. 


#

- 개발과정 중에 보고싶은 데이터가 있으면

  div 태그 클래스 값 주고 비워두고 아래처럼 코딩.

```javascript
  tableObj.find("tbody tr").hover(
    function(){
      // $(this).attr("bgcolor", "pink")
      $(".msg").html("<div>마우스 댔니?</div>")
    }
    ,function(){
      // $(this).attr("bgcolor", "lightgray")
      $(".msg").html("<div>마우스 뻈니?</div>")

    }	
  );




	<div class="msg"></div>
```



----
### `17:30~`

## # JAVA 진도.  

생성자 오버라이딩 오버로딩 

#

`static` (입)

```
---------------------------------
static
---------------------------------
    시험문제 타겟!
    --------------------------------------------------------------------
    <의미1> static 이 붙은 속성변수는 객체생성없이 아래 처럼 호출이 가능하다. 
    --------------------------------------------------------------------
        ---------------------------------
        클래스명.속성변수
        ---------------------------------
            ---------------------------------
            <참고> 원래 속성변수나 메서드는 아래와 같은 절차를 거쳐 호출이 된다.  
            ---------------------------------                 
                클래스명    객체참조변수명  =   new 생성자명(~);
                객체참조변수명.속성변수
                객체참조변수명.메서드명(~)
            ---------------------------------
    --------------------------------------------------------------------
    <의미2> static 이 붙은 속성변수를 가진 클래스를 new 를 사용해여 2개 이상 객체화 했을 때
            객체끼리 이 static 이 붙은 속성변수를 서로 공유한다.   
    --------------------------------------------------------------------
```

> 객체 생성없이 `클래스명.속성변수` 가능하다.  

> ![static설명1](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210728%201716_JAVA_static1_.png)    

> ![static설명2](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210728%201718_JAVA_static2_.png)   

> ![static설명2](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210728%201735_JAVA_static3_.png)     

#

`final`  (입)

```
---------------------------------
final       (입)
---------------------------------
    시험문제 타겟!
    --------------------------------------------------------------------
    <의미> final 이 붙은 속성변수는 처음 데이터를 저장한 후에는 절대 수정이 불가능하다.
    --------------------------------------------------------------------
    불변규칙, 과학적 불변 데이터 인 경우 수정을 불가능하게 하기 위함이다.  
    <예> 원주율 3.14, 회사규칙 중 불변, 직원수는 10000이하.
    --------------------------------------------------------------------
    <주의> final 이 붙은 속성변수는 자동 초기화가 될 수 없다.  
            그러므로, 속성변수를 선언할 때 데이터를 저장하거나,
            생성자 안에서 저장하는 코딩이 반드시 나와야 한다.  

    <주의> final 이 안 붙은 속성변수는 자동 초기화가 될 수 있다.  
```
> `final` 은 금지의 의미가 내포되어 있다.    

> `final` 이 붙은 속성변수는 수정이 가해지려하면 오류가 난다.  

> ![final](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210728%201756_JAVA_final_.png)    


> 속성변수는 선언할때 값을 넣지 않으면 자동으로 0으로 들어간다.  
> JAVA는 속성변수에 무조건 데이터가 들어가야 한다.  

> `final` 붙인 속성변수는
> 속성변수 선언할때 넣어주거나 
> 생성자 안에서 속성변수 값을 넣어 줄 수 있다.  

> `final` 이 붙은 속성변수는 자동으로 넣어주지 않는다.  
> `final` 붙여서 잠그는 이유는?  
> 유저가 원하는 데이터를 넣고 잠그는 것이기 때문에.    
> JAVA가 자동으로 넣어서 잠그면 의미가 없다.  


### # `final` 나오는 세가지이유.

- **속성변수** : 수정, 갱신금지

- **메서드**  : 오버라이딩 금지

- **클래스앞** : 상속금지 (자식클래스를 가지지 않겠다)

----
### `18:30~`
