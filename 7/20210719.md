# 2021년 7월 19일 [47일차]

## # 오전수업
----
### `09:30~`

반복, 복습. 중요.   

기본적인 패턴 암기 필요.  

<복습>   

지난주 java 수업.      

웹개발에 필요한, 자바공부 우선적으로.    

코딩에 관련된 문법.  

#### # Javascript 교제.    

- DOM 객체,  

- JQuery 객체 ★★★,  

  JQuery 객체의 메위주 얻는 방법 예시가 중요.    
  JQuery 함수를 호출하면 JQuery 객체의 메위주를 리턴받는다.     
  선택자("문자열") = 태그를 지칭.    
  JQuery 객체 메소드 호출하기 위한 절차.  

  ```  
  <주의>  
  
  JQuery => 지칭할때는 대문자 사용.  
  jQuery => 코딩 할 때는 첫글자 소문자 사용.  
  선택자 => 태그를 지칭하는 방법론.  
  ```  


- 사용자 정의 객체,

  솔루션 회사에서 주로 사용.  

  객체 생성할 목적으로 사용.    

#  

#### # `javascript_교재.txt` 진도.      

#### # `사용자 정의 객체` 이어서...

```javascript
function Sungjuk(s_no, s_name, kor, eng, mat){  // 과목수와 상관없이 하려면 Array 사용.
    // ---------------------
    // 속성변수 선언
    // ---------------------
    this.s_no = s_no;
    this.s_name = s_name;
    this.kor = kor;
    this.eng = eng;
    this.mat = mat;

    // ---------------------
    // 메소드 선언
    // ---------------------
    this.getS_no = function( ) {
        return this.s_no;
    }
    this.getS_name = function( ) {
        return this.s_name;
    }
    this.getKor = function( ) {
        return this.kor;
    }
    this.getEng = function( ) {
        return this.eng;
    }
    this.getMat = function( ) {
        return this.mat;
    }
    this.getTot = function( ) {
        return this.kor + this.eng + this.mat;
    }
    this.getAvg = function( ) {
        return this.getTot()/3;
    }
}

  // 함수명이 대문자이면 대부분 생성자 함수.  
  // 객체 생성의 목적은 메소드 호출.  
  // 코딩 형태가 변수선언이라도 익명함수가 들어있으면 메소드이다.  
  // 익명함수가들어있는 변수는 메소드.     
```

- 자스에 `단위프로그램` (함수, 객체)  
  > 프로그램구성 `최소단위`. 쪼갤수 없는 최소한의 기능적 묶음, 모임.

  > 자바는 객체중심, 자스에서는 함수, 객체.  
  
  ```
  ○ 함수 : 실행구문에 블럭으로 묶고, 이름만 붙임.    

  ○ 객체 : 함수보다 더 조직화된 단위프로그램. 
           [속성변수(멤버변수), 메소드] 
  ```

  ![단위프로그램설명](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210719%201007_Javascript_%EB%8B%A8%EC%9C%84%ED%94%84%EB%A1%9C%EA%B7%B8%EB%9E%A8%20%EC%84%A4%EB%AA%85_.png)  

----
### `10:30~`

```javascript

// 위 코딩 이어서.  

  var sungjuk = new Sungjuk(1, "사오정", 91, 81, 71);      // 생성자 함수 객체화.
  alert( sungjuk.getTot() )       // 총점 계산 메소드 경고창 출력.
  alert( sungjuk.getAvg() )       // 평균 계산 메소드 경고창 출력.  

```

![객체화_매개변수](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210719%201040_%EB%8B%A8%EC%9C%84%ED%94%84%EB%A1%9C%EA%B7%B8%EB%9E%A8_%EA%B0%9D%EC%B2%B4%ED%99%94_%EB%A7%A4%EA%B0%9C%EB%B3%80%EC%88%98_.png)      


```
**************************
생성자 함수를 객체화 하는 코딩 형식
**************************
  ------------------------------------------
  var 변수명 = new 생성자함수명( [데이터1] );   // 변수명에는 객체의 메위주가 들어있다
  ------------------------------------------
  // 생성자함수명과 변수명을 혼동하면 안된다.  
      <1> 변수를 선언한다.
      <2> new 에 의해 지정한 생성자함수를 메모리 공간으로 올려 객체화 한다.
      <3> 메모리에 올려진 생성자 함수를 생성자함수( [데이터1] ) 형식으로 호출한다.
          이때 매개변수가 있다면 데이터1 이 매개변수 로 전달된다.
      <4> [객체의 메모리 위치 주소값]을 리턴하여 [변수]에 저장한다.    
```

```
속성변수, 메소드 앞에 `this.`을 붙인다.  

변수안에 `익명함수`를 대입하여 호출하면 `메소드`가 된다.  
```

#

### <24> 자바스크립트 [일회성 사용자 정의 객체]에 대해 설명한다면? 

일회성 사용자 정의 객체는 코딩상 재사용이 불가능하다.  

```
● [생성자함수] 없이 만들어지는 객체를 말한다.
● 일회만 사용할 목적으로 만들어지는 객체이기도 하다.  

**************************
[일회성 사용자 정의 객체] 생성 코딩 형식
**************************
  ------------------------------------------
  var 변수 = {
      속성변수명 : 데이터,
      메소드명 : function([매변2]){
          실행코드;
          [return [데이터];]
      }
  }
  ------------------------------------------
    속성변수는 0개 이상 선언가능
    메소드는 0개 이상 선언가능.
    실무적으로 본다면, 속성변수나 메소드는 1개이상 선언해야 한다.  
    실무적으로 [일회성 사용자 정의 객체] 안에 메소드는 잘 나오지 않는다. 속성변수만 사용하려고 씀. 
    주로 여러 데이터  한속에 모아 보관하고 사용할 목적으로 사용한다.  
  ------------------------------------------

**************************
[일회성 사용자 정의 객체] 의 메소드/속성변수
**************************
    변수.속성변수
    변수.메소드명(~)
```

- [일회성 사용자 정의 객체] 생성코딩
  
  ```javascript
  var sungjuk = {
      // ---------------------
      // 속성변수 선언
      // ---------------------
      s_no : 1
      ,s_name : "사오정"
      ,kor:91
      ,eng:81
      ,mat:71

      // ---------------------
      // 메소드 선언
      // ---------------------
      ,getS_no : function( ) {
          return this.s_no;
      }
      ,getS_name : function( ) {
          return this.s_name;
      }
      ,getKor : function( ) {
          return this.kor;
      }
      ,getEng : function( ) {
          return this.eng;
      }
      ,getMat : function( ) {
          return this.mat;
      }
      ,getTot : function( ) {
          return this.kor + this.eng + this.mat;
      }
      ,getAvg : function( ) {
          return this.getTot()/3;
      }
    }
  ```

----
### `11:30~`  

자스에서    
객체화가 가능한 함수를 [생성자함수]라고 한다.    

자바에서    
[생성자]와는 다르다.    


```javascript
// 실무적 코딩  
*************************************************
다량의 데이터를 저장해서 관리하는 방법
*************************************************
-------------------
방법1> 데이터마다 일일이 변수 다 만들어 저장한다.  (원시적 방법)
-------------------
    var kor = 91;
    var eng = 81;
    var mat = 71;
    var che = 61;
    var phy = 51;
    alert(kor)
    ------------------------------------------------------------
    변수에서 데이터를 꺼내는 방법은 변수명을 일일이 코딩하면 된다.
    다른 프로그램 쪽으로 데이터를 전달하려면 모두 전달해 주면 된다.
    그럼 받는쪽도 n개의 변수로 받아야 한다.(=비효율적)
    ------------------------------------------------------------
-------------------
방법2> Array 객체에 저장한다.
-------------------
    var jumsu = [91,81,71,61,51];
    alert(jumsu[0])
    ------------------------------------------------------------
    Array 객체에서 데이터 꺼내는 방법은 반복문 사용하여 jumsu[i] 을 코딩하면 된다. 
    다른 프로그램 쪽으로 데이터 전달하러면 Array 객체의 메위주만 전달해 주면 된다.  
    치명적 단점. 데이터가 무슨 데이터인지 알기가 힘들다.
    Array 객체에 데이터를 저장할 땐 단순한 일방적인 데이터를 저장한다. 
    예>1000개의 사과 무게를 저장할 때
        var appleWeight = [200,210,190,~~~]
-------------------
방법3> [일회성 사용자 정의 객체]에 저장한다.
-------------------
    var jumsu = {kor:91,eng:81,mat:71,che:61,phy:51};
    alert(jumsu.kor)
    ------------------------------------------------------------
    Array 객체에서 데이터 꺼내는 방법은 jumsu.kor 의 형식으로 코딩하면 된다. 
    다른 프로그램 쪽으로 데이터 전달하려면 [일회성 사용자 정의 객체]의 메위주만 전달해 주면된다.
    장점. 데이터가 무슨 데이터인지 알 수가 있어 꺼내기 쉽다.  
    단점. 반복문 사용이 힘들다.
    ------------------------------------------------------------

-------------------

일반 array 사용하면 변수명이 획일적이기 때문에 
[일회성 사용자 정의 객체]를 사용.  

과목명이 다 다르기 때문에 [일회성 사용자 정의 객체]를 사용.

같은 종류의 데이터를 쓴다면 array가 더 적합하다. ex) 사과의 무게.
```

오라클 dept 테이블 4행 3열. 데이터 자스로 끌어오는 방법?  

한행은 [일회성 사용자정의 객체] 사용.  

여러행은 Array 객체 사용.    

----
### `12:30~`

```
-------------------
<새끼문제> 오라클 dept 테이블에 다음과 같이 데이터가 저장되어 있을 경우 
          자스영역으로 데이터를 끌고와서 관리한다면 어떻게?
-------------------
    ------------------------------
    dep_no      dep_name    loc
    ------------------------------
    10          총무부       서울
    20          영업부       부산
    30          전산부       대전
    40          자재부       광주
    ------------------------------

    var dept = [
        { dep_no:10, dep_name:"총무부", loc:"서울" }
        ,{dep_no:20, dep_name:"영업부", loc:"부산"}
        ,{dep_no:30, dep_name:"전산부", loc:"대전"}
        ,{dep_no:40, dep_name:"자재부", loc:"광주"}
    ];

    ---------------------------------------------------
    40번 부서의 이름 꺼내는 방법?    dept[3].dep_name
    부서의 개수?                    dept.length
    ---------------------------------------------------
    아래 같이 2차원 Array 객체를 사용하면 각 데이터가 무얼 의미하는지 모르므로 좋은 방법은 아니다.

    // 이 방법은 위의 방법과 달리, 좋은 방법이 아니다.
    // 데이터 성분을 알아보기 힘들다.  
    var dep = [
        [10, "총부무", "서울"]
        ,[20, "영업부", "부산"]
        ,[30, "전산부", "대전"]
        ,[40, "자재부", "광주"]   
    ];
```

- [일회성사용자정의] 에서의 Array 객체
  
  ![일회성사용자정의_Array객체](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210719%201248_%EC%9D%BC%ED%9A%8C%EC%84%B1%EC%82%AC%EC%9A%A9%EC%9E%90%EC%A0%95%EC%9D%98_Array_.png)      

  > dept 안에 Array 객체가 들어있고, 그안에 `사용자정의 객체`의 `메모리 위치 주소값(메위주)`이 들어있다.  
  > dept 메위주를 사용하여 그 안의 `사용자정의객체`까지 다 사용할 수 있다.    


> `javascript_교재.txt` Array 객체 다시 보기.  

----
### `13:20~`

  - 점심시간.

---
---

## # 오후수업

---
### `14:30~`

```javascript
사용자 정의 객체 안에

  var jumsu = {kor:91,eng:81,mat:71,che:61,phy:51, kor:100};

같은 kor 속성변수를 넣으면,

오류가 나지 않고, 덮어쓰우기가 된다.  

마지막 kor이 원본이 된다.  
```

#

### <25> [JSON] 이 뭡니까? ★★★★ 솔루션회사 면접질문. (입) 

```
● 동종 또는 이기종 S/W 끼리 쉽게 데이터를 전달하기 위해 만든 [개방형 표준 데이터 표기법]을 말한다.
● 일회성 사용자 정의 객체 표현법과 동일하다. 
● JSON = Javascript Object Notation = 자바스크립트 객체 표기법
    <참고> JavaScript 에서 일회성 사용자 정의 객체 표현법과 동일하여 만들어진 이름이다. 
```

![JSON설명이미지](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210719%201446_JSON_.png)     


> 상업적기반의 파생효과를 낼 수 있는것, 기반 = platform    

```
**************************
● JSON 특징
**************************
  각종 프로그래밍 언어나 플랫폼(=platform)에 종속되지 않는 독립형 데이터 포맷이다.
      -----------------------
      <참고> 플랫폼(=platform) => 특정 서비스를 하기 위한 기반. 보통 플랫폼은 무료, 플랫폼이란 기반 위의 서비스는 유로.
      -----------------------
**************************
● JSON 장점
**************************
  다양한 프로그래밍 언어에서 동종 또는 이기종 S/W 끼리 쉽게 데이터를 전달할 수 있다.
  스프링 같은 프레임워크 사용시 웹브라우저에서 [비동기 방식]으로 서버와 데이터를 주고받을 때 JSON 사용하기도 한다.  
      -----------------------
      <참고> 비동기 방식?
      -----------------------
          ▷ 클라이언트의 웹브라우저에서 화면이동(=페이지 이동) 없이 AJAX 기술로 웹서버와 통신하는 방식을 말한다.
          ▷ <예> 아이디 입력 후 버튼을 눌러 페이지 전환 없이 서버와 통신하여 아이디 중복여부를 수신 받는 경우
          ▷ <예> 네이버에서 검색 키워드 입력 시 페이지 전환 없이 서버와 통신하여 관련 검색어가 밑에 뜨는 경우
          ▷ <예> 네이버 지도에서 지도를 밀면 새로운 지도가 나오는 경우
      -----------------------
      <참고> 동기 방식?
      -----------------------
          ▷ 클라이언트의 웹브라우저에서 화면이동(=페이지 이동)을 하면서 웹서버와 통신하는 방식을 말한다.
          ▷ 전통적인 웹서버와의 통신 방법이다.  

**************************
● JSON 형식과 JSON 안의 키값에 대응하는 데이터 호출방법.  
**************************
  -----------------------
  JSON 형식
  -----------------------
  var 변수명 = {
      "키값1" : 데이터
      ,"키값2" : 데이터
      , ~
      ,"키값n" : 데이터
  }
  -----------------------
      <주의> 키값는 서로 달라야한다.  키값이 같으면 마지막이 진짜다. 
      <주의> 데이터는 주로 문자열 또는 숫자 이다. 즉, 모든 프로그래밍 언어가 고유하는 데이터이다.
  -----------------------
  JSON 안의 키값에 대응하는 데이터 호출형식
  -----------------------
      변수명["키값"]

  -----------------------
  <예>
  -----------------------
  var windowAirCondition = {
      "제품번호" : 1
      ,"제품이름" : "창문형에어컨(PWA-3300WP)"
      ,"제조사" : "파세코"
      ,"재고물량" : 300 
  }
  -----------------------
  alert( windowAirCondition["제품이름"] )
```

---
### `15:30~`

```
<JSP 6대 기술>
--------------------
Directive
EL
Custom Tag
표준 액션 태그 (x)
JSP Script (x)
Implicit Object (x) HttpServletRequest, HttpServletResponse, HttpSession
--------------------

JSP : Java Server Page
(x)는 요즘 잘 사용하지 않는다.  
```

HTML 소스 위주로 보면서 적용복습을 해야한다.  


`loginForm.html`   

`boardList.html`  body 태그 수정.  

#

### <26> `<body onload="openMessage()">` 이 코드가 뭘 의미하나?

```
------------------------------------------------------
웹브라우저가 body 태그 안의 태그들을 모두 읽어 들인 후에 
자바스크립트의 openMessage() 라는 함수를 호출해라.
------------------------------------------------------
------------------------------------------------------
<참고> <태그명 on이벤트="자스코드"> 이 코드의 의미? 
------------------------------------------------------
    이 태그에 on 다음에 나오는 이벤트를 일으키면 자스코드를 실행하라.
```

#### # 현재시점의 요일을 로그인하면 출력되게 만들기.  

- `boardList.html` 에 공용함수 `common_func.js` 수입하고,  

- `boardList.html` 에서 body 태그 수정 후, script 에서 함수 만들어주기.  

  ```html

  <script>
    function openMessage(){
      var week = getTodayWeek();
      alert(week);
    }
  </script>

  <body onload="openMessage()">

  ```

`common_func.js`에 `getTodayWeek()` 공용함수 만들어준다.  

```javascript
//------------------------------------------------------------------------
// 현재시점 날짜의 요일
//------------------------------------------------------------------------

function getTodayWeek(){
  var today = new Date();
  var weekNo = today.getDay();
  var week = "일요일";
  if( weekNo==1 ){
    week = "월요일";
  }
  else if( weekNo==2 ){
    week = "화요일";
  }
  else if( weekNo==3 ){
    week = "수요일";
  }
  else if( weekNo==4 ){
    week = "목요일";
  }
  else if( weekNo==5 ){
    week = "금요일";
  }
  else if( weekNo==6 ){
    week = "토요일";
  }
  return week;
}
```

----
### `16:30~`

- 주석달기.

  ```javascript
  //------------------------------------------------------------------------
	// 현재시점 날짜의 요일
	// 현재 시각의 요일을 리턴하는 함수 선언. 
	//------------------------------------------------------------------------
	
	function getTodayWeek(){
		// ------------------------------
		// today 변수 선언. 현재 날짜를 관리하는 Date 객체 생성. Date 객체의 메위주를 today변수에 저장.
		// ------------------------------
		var today = new Date();
		// ------------------------------
		// weekNo 변수 선언. Date 객체의 getDay 메소드 호출하여 현재 요일 번호 얻어 weekNO 변수에 저장하기
		// ------------------------------
		var weekNo = today.getDay();
		// ------------------------------
		// week 변수 선언. "일요일" 저장하기 
		// ------------------------------
		var week = "일요일";
		// ------------------------------
		// 요일 번호에 따라 week 변수에 오는 요일문자 저장하기.
		// ------------------------------
		if( weekNo==1 ){
			week = "월요일";
		}
		else if( weekNo==2 ){
			week = "화요일";
		}
		else if( weekNo==3 ){
			week = "수요일";
		}
		else if( weekNo==4 ){
			week = "목요일";
		}
		else if( weekNo==5 ){
			week = "금요일";
		}
		else if( weekNo==6 ){
			week = "토요일";
		}
		// ------------------------------
		// 오늘 오일 문자 리턴하기
		// ------------------------------
		return week;
	}
	/*
	//------------------------------------------------------------------------
	// 위의 요일 리턴 함수와 똑같은 출력을 하는 함수 형식이다.  
	//------------------------------------------------------------------------
	function getTodayWeek(){
		return["일요일","월요일","화요일","수요일","목요일","금요일","토요일"][new Date().getDay()]
		// 위는 new Array("일요일","월요일","화요일","수요일","목요일","금요일","토요일")[1] 같은 형식이다.
	}
	*/
  ```
  > 참고, 똑같은 함수가 공용함수에 들어가 있으면 마지막것이 진짜가 된다.  

#### # 요일별 특정 메세지 경고창 띄우기.  

- JSON형식 요일별 특정 메세지 `common_func.js`에 작성.

  ```javascript
	//------------------------------------------------------------------------
	// JSON 선언.
	//------------------------------------------------------------------------
  var weekMsg = {
	"월요일" : "월요일은 9시에 회의가 있는 날입니다. 보고서 준비해 주십시오."
	,"화요일" : "화요일은 대청소 날입니다." 
	,"수요일" : "수요일은 봉사활동 날입니다."
	,"목요일" : "부서별 회식이 있습니다."
	,"금요일" : "오후 12시에 대강의실에서 상 수여식이 있습니다."
  }	
  ```

- `boardList.html` 에 코드 추가    

  ```javascript
  function openMessage(){   // 요일별 메시지를 보여주는 openMessage() 함수 선언.
    
    var week = getTodayWeek();  // week 변수 선언. 
                                // getTodayWeek() 함수 호출로 현재요일 얻어 week 변수에 저장하기
    
    var msg = weekMsg[week];    // msg 변수 선언.
                                // 공용함수에 weekMsg 변수에 저장된
                                // JSON 객체 안에서 week 변수안의 문자를 키값으로 가지고 있는 놈의 대응 문자 얻어
                                // 변수 msg 에 저장하기.
  
    alert(msg);                 // alert 로 msg 출력. 
  }
  ```

#### # `boardList.html`에 현재 시각의 년-월-일(요일) 나오게 하기.  

- `common_func.js`에 년-월-일(요일) 공용함수 추가.  

  ```javascript
  //------------------------------------------------------------------------
	// 현재 시각의 년-월-일(요일)을 문자로 리턴하는 함수 선언.
	//------------------------------------------------------------------------
	function getTodayYMDW(){
		// ------------------------------
		// today 변수 선언. 현재 날짜를 관리하는 Date 객체 생성. Date 객체의 메위주를 today변수에 저장.
		// ------------------------------
		var today = new Date();
		// ------------------------------
		// 현재 시각의 년-월-일(요일) 을 문자로 리턴하기.
		// ------------------------------

		return today.getFullYear() + "-" + (today.getMonth()+1) + "-" + today.getDate() + "(" + getTodayWeek().replace("요일","") + ")";
	}
  ```

- `boardList.html` 에 년-월-일(요일) 코드 추가      

  ```javascript
  document.write(getTodayYMDW());
  ```

----
### `17:30~`

#### # 공용함수와 공용 JSON 데이터 분리하기.  

- `common_func.js`에 들어있는 JSON 선언 오려내기.    

- `common_date.js` 파일 새로 만들어서 오려낸 JSON 선언 붙여넣기.  

- `common_date.js` 의 JSON 선언 사용을 위해서  

- `boardList.html` 에 파일 수입하기.  

  ```html
  <script src="common_data.js"></script>
  ```

----
### `18:30~`     

내일 기념일, D-Day 등등     

내일 오후 자바문법. 추가로 나감.    

입사시험 따로 정리하기.       

HTML 문서 코드 분석하면서 공부하기.         

내일 오후, 수요일 쯤 JQuery(선택자, 메소드) 들어감.         

JQuery 다 하게되면 CSS 나감.      

그뒤에는 JSP.  back-end      


