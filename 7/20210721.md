# 2021년 7월 21일 [49일차]

## # 오전수업
----
### `09:30~`

#### # 오늘 기준으로 특정날짜가 10일 안으로 걸려들면 D-Day 출력하기. [(어제 이어서)](https://github.com/SungWoo0315/study-repository/blob/main/7/20210720.md#-%EC%98%A4%EB%8A%98-%EA%B8%B0%EC%A4%80%EC%9C%BC%EB%A1%9C-%ED%8A%B9%EC%A0%95%EB%82%A0%EC%A7%9C%EA%B0%80-5%EC%9D%BC-%EC%95%88%EC%9C%BC%EB%A1%9C-%EA%B1%B8%EB%A0%A4%EB%93%A4%EB%A9%B4-d-day-%EC%B6%9C%EB%A0%A5%ED%95%98%EA%B8%B0)   


- catch 구문 안에 있는것은 예외가 발생했을 때 실행된다.  

  `boardList.html`      
  `common_func.js`    
  두개 파일에,  
  주석달기.  

- getTime() 메소드 설명.

  ![getTime()](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210721%201006_Date_getTime_.png)    

----
### `10:30~`

- `boardList.html`의 함수들 주석 달기.

  ```javascript

  // 누적시켜서 출력하는 다른 방법. Array 사용해서 배열안에 누적시켜서 출력하기.  
  // -------------------------------------------------
  // common_date.js 파일 안 scheduleMsg 변수안의 2차원 Array 객체에 접근해서
  // 오늘 날짜와 일치하는 놈의 메시지들을 모아서 리턴하는 함수 
  // -------------------------------------------------
  function getScheduleMsg(){
      // ---------------------------
      // 메시지를 저장할 Array 객체 생성하기.
      // ---------------------------
      var arr = [];
      // ---------------------------
      // scheduleMsg 변수안의 2차원 Array 객체에 접근해서
      // 오늘날짜와 일치하는 놈의 메시지들을 모아서 Array 객체에 저장하기
      // ---------------------------
      for( var i=0; i<scheduleMsg.length; i++){
          var date = scheduleMsg[i][0];
          date = deleteBlank(date);  // 공백제거 공용함수 사용.  
          var msg = scheduleMsg[i][1];
          if( isToday(date) ){
              // alert( msg ) // 테스트 할때 보려고 alert 한것. 주석처리.
              arr.push(msg); // arr 안에 누적시키기. 
                             //특정날짜에 메시지가 2개 이상 나올 경우 누석시켜 다 나오게 하기.
          }
      }	
      // ---------------------------
      // Array 객체에 저장된 메시지를 모두 붙여 문자로 리턴하기.
      // ---------------------------
      return arr.join(" <br><br> ");	// arr 에 join 사용해서 배열 붙여서 출력하기
                                      // 중간에 <br>을 넣어주어 이어 붙인 사이에 줄바꿈을 해준다.
  }

  ```

  ```javascript

  // -------------------------------------------------
  // 명언을 랜덤하게 나오게 하기.
  // 명언을 리턴하는 함수 선언.
  // 명언 문자열은 common_date.js 파일안 goldenSaying 변수안에 저장되어 있다.
  // -------------------------------------------------
  function get_goldenSaying(){
      // ---------------------------
      // goldenSaying 변수안에 저장된 Array 객체 안의 배열변수 개수 얻기
      // 이 개수가 명언의 개수이다.   
      // ---------------------------
      var goldenSaying_cnt = goldenSaying.length;
      // ---------------------------
      // 0 ~ 명언개수-1 사이의 무작위 인덱스 번호 얻기.
      // 이 인덱스번호 무작위 배열 변수를 선택할 때 사용된다.
      // ---------------------------
      var random_index = Math.floor( Math.random()*goldenSaying_cnt );
      // ---------------------------
      // 무작위 인덱스 번호에 해당하는 배열변수 안의 데이터를 리턴하기.
      // ---------------------------
      return goldenSaying[random_index];
  }

  ```

  ```javascript

  // -------------------------------------------------
  // D-Day 나오게 하기.
  // 오늘부터 특정 날까지 남은날이 10일 이하일 때 그 특정 날짜에 해당하는 메시지들을 리턴하는 함수.
  // 특정 날짜와 메시지는 common_date.js 파일안 dDayMsg 변수안에 저장되어 있다.
  // -------------------------------------------------
  function get_DDayMsg(){
      // ---------------------------
      // D-Day에 대응하는 메시지를 저장할 Array 객체 생성하기.
      // ---------------------------
      var arr = [];
      // ---------------------------
      // dDayMsg 변수안의 2차원 Array 객체에 접근해서
      // D-Day가 10일 이하일때의 메시지들을 모아서 Array 객체에 저장하기.
      // ---------------------------
      for( var i=0; i<dDayMsg.length; i++){
          var date = dDayMsg[i][0];
          date = deleteBlank(date);  // 공백제거 공용함수 사용.  
          var msg = dDayMsg[i][1];

          // ---------------------------
          // get_dDay 함수 호출하여 오늘부터 Array 객체 안의 i번째 날짜까지의 남은 일수 구해서
          // 변수 dDay 에 저장하기.
          // ---------------------------
          var dDay = get_dDay(date);

          // ---------------------------
          // 구한 dDay 가 0~10 일 사이면 Array 객체안에 저장.
          // ---------------------------
          if( dDay>=0 && dDay<=10 ){
              arr.push(date + " 까지 " + msg + " 은(는) " + dDay + " 일 남았습니다."); // arr 안에 누적시키기. 
                              //특정날짜에 메시지가 2개 이상 나올 경우 누석시켜 다 나오게 하기.
          }
      }
      // ---------------------------
      // Array 객체에 저장된 메시지들이 1개 이상이면 모든 메시지를 붙여 리턴하기.
      // ---------------------------
      if (arr.length>0){
          return arr.join(" <br> ");	
      }
      // ---------------------------
      // Array 객체에 저장된 메시지들이 0개면 "" 리턴하기.
      // ---------------------------
      else{
          return "";
      }	
  }

  ```

- 소스중심의 문제를 내면서 javascript_교재.txt 정리 할 예정.

----
### `11:30~`

`javascript_교재.txt` 추가 진도 나감.  

### <27> 자스에서 `var arr = [];` 이 코드가 무엇을 의미할까?

```
  --------------------------------------------
  arr 변수 선언하기,
  Array 객체 생성한 후 Array 객체의 메위주를 arr 변수에 저장하기 
  이때 Array 객체 안에는 배열변수 0개이다. 즉, 배열 변수는 없다. 추후에 저장용으로 생성.
  다량의 데이터를 저장할 목적으로 Array 객체를 생성해 놓은 것이다.
  --------------------------------------------
  위 코드는 아래와 같은 코드 이다.
  var arr = new Array();
```

> <b>변수에 데이터를 저장하는 이유? 재사용 때문.</b>

#

### <28> 자스에서 `var arr = [1,3,5];` 이 코드가 무엇을 의미할까?

```
  --------------------------------------------
  <1> arr 변수 선언하기,
  <2> Array 객체 생성한 후 Array 객체의 메위주를 arr 변수에 저장하기 
      이때 Array 객체 안에는 배열변수 3개가 있다. 
      3개의 배열변수 안에는 1, 3, 5 가 저장되어 있다.
  --------------------------------------------
  각 배열 변수를 호출하는 방법은      arr[0]  arr[1]  arr[2]      이다. 
  대괄호 안에 인덱스 번호를 쓸수 있으므로 
  반복문으로 돌리면서 이 인덱스번호를 바꾸어 접근할 수 있다.
```

#

### <29> 자스에서 `var xxx = getDdd(3);` 이 코드가 무엇을 의미할까?

```
  --------------------------------------------
  <1> xxx 변수 선언하기,
  --------------------------------------------
  <2> getDdd 라는 이름의 함수를 getDdd(3) 식으로 호출하기.
      getDdd 함수호출하면서 3이란 데이터를 넘겨준것이다.
  --------------------------------------------
  <3> getDdd 라는 이름의 함수를 호출하여 리턴받은 데이터를 xxx 변수에 저장하기.  
      그 함수는 function getDdd( 매개변수명 ){~; return 데이터;} 의 형식을 가지고 있을 것이다.
  --------------------------------------------
  <주의> 함수를 호출한 지점이 `=` 오른쪽이다. `=` 오른쪽은 데이터가 나와야 하는 곳이므로,
          이 함수는 호출하면 반드시 데이터를 리턴해야 할 것이다.
  <참고> 관용적으로 get 으로 시작하는 함수는 리턴값이 있다.
  <참고> 관용적으로 set 으로 시작하는 함수는 리턴값이 없다. 
```

#

### <30> 자스에서 `var xxx = new Date();` 이 코드가 무엇을 의미할까?

```
  --------------------------------------------
  <1> xxx 변수 선언하기,
  --------------------------------------------
  <2> Date 객체 생성한 후 Date 객체의 메위주를 xxx 변수에 저장하기. 
      이때 Date 객체는 현재 년월일시분초를 관리한다.
      현재란 지금 이 시점의 년월일시분초를 말한다. 즉, 흘러가는 년월일시분초를 말한다.  
  --------------------------------------------
  객체 생성 후 아마   xxx.메소드()    형식으로 Date 객체의 메소드를 호출할 것이다. 
  객체 생성의 목적은 메소드 호출이다.
```

#

### <31> 자스에서 `var xxx = new Date(1999, 11, 25);` 이 코드가 무엇을 의미할까?

```
  --------------------------------------------
  <팁> 쉽게 월 구분하는 방법
        var xxx = new Date(1999, 12-1, 25);
  --------------------------------------------
  <1> xxx 변수 선언하기,
  --------------------------------------------
  <2> Date 객체 생성한 후 Date 객체의 메위주를 xxx 변수에 저장하기. 
      이때 Date 객체는 1999년 12월 25일 0시 0분 0초를 관리하고 있다.
      고정된 날짜인 1999년 12월 25일 0시 0분 0초를 관리하고 있다.
  --------------------------------------------
  <주의> Date 객체 생성시 월은 사람이 생각하는 월에서 -1 한 월을 세팅한다.
          Date 객체는 월을 0~11까지의 숫자로 관리하고 있기 때문이다.
  --------------------------------------------
  <요령> 월 셋팅 헷갈리므로 아래 처럼 하면 덜 헷갈린다.
          ------------------------------------ 
          var xxx = new Date(1999, 12-1, 25);
          ------------------------------------ 
```

#

```javascript
<<Tip>>

쉽게 월 구분하는 방법

  var xxx = new Date(1999, 12-1, 25);
```

#

### <32> 자스에서 `var xxx = new Date(1999, 13, 25);` 이 코드에서 Date 객체가 관리하는 년월일 도데체 뭡니까?

```
  위 코드를 아래처럼 변경하면
      -----------------------------------------
      var xxx = new Date(1999, 11+2, 25); 이다.
      -----------------------------------------
  11은 12월을 의미한다. 
  12월에 2개월 더한 것과 마찬가지이므로 결국,
  2000년 2월 25일 0시 0분 0초 를 관리한다.      
  <참고>시분초가 없으면 0시 0분 0초 가 세팅된 것과 동일하다.
```

> 년 월 일은 더하거나 빼는 연산작업을 할 수 있으므로 숫자 자료형을 써주는것이 정석이다.

> 자동으로 형변환이 되어서 ""(더블쿼트) 사용해서 넣어도 정상작동 되기는 한다.

> 공부할때는 올바른 자료형을 사용하는것이 좋다.  

#

### <33> 자스에서 `var xxx = function(a){~};` 이 코드가 무엇을 의미할까?

```
  --------------------------------------------
  <1> xxx 변수 선언하기,
  --------------------------------------------
  <2> 익명함수 function(매개변수){실행구문} 를 xxx 변수에 저장하기. 
  --------------------------------------------
      --------------------------------------------
      자스는 익명함수. 즉, 이름없는 함수도 데이터 취급을 하므로 변수에 저장이 가능하다.
      --------------------------------------------
      익명함수도 함수이므로 호출이 목적이다. 
      --------------------------------------------
      익명함수를 변수에 저장하므로 이동하기 쉽고 필요시 익명함수를 호출할 수 있다.
      --------------------------------------------
      변수에 저장된 익명함수 호출 방법은 아래와 같다.  
          -----------------------
          변수명( [데이터] )  // 데이터는 생략 가능.
          -----------------------
  --------------------------------------------
  <3> xxx 변수안의 익명함수 호출방법은 아래와 같다. 
  --------------------------------------------
      xxx(데이터)
```

> 이름을 주지 않은 함수 = `익명함수`

> 자스에서는 익명함수도 데이터 취급을 해서 변수안에 집어 넣을 수 있다.   

----
### `12:30~`

### <34> HTML 파일 안에서 `<script src="common_func.js"></script>` 이 코드가 무엇을 의미할까?

```
  HTML 파일의 head 태그 안에 script 태그 를 생성하고, 
  common_func.js 안에 있는 공용함수를 사용하기위해, 
  수입할 html 파일이 있는 같은 폴더에 common_func.js 파일을 위치시키고 이 파일을 불러온다.

  --------------------------------------------
  common_func.js 파일안의 자바스크립트 소스를 복사해서 현 script 태그 사이에 삽입하라.
  --------------------------------------------
  그러므로 <script src="common_func.js"></script> 를 가지고 있는 html 파일안에 
  common_func.js 파일안의 자바스크립트 소스가 있는 것과 동일하다.
  --------------------------------------------
  src="common_func.js" 의미는 현재 이 html 파일의 저장 폴더와 동일한 폴더에 common_func.js 파일도 존재한다는 의미이다. 
  즉, JS 파일 이름만 나온다면 동일한 폴더에 들어 있다 라는 의미이다.
```


### <35> 호출할 함수는 함수호출 코드 위에만 나와야하는가? 즉, 함수를 선언한 후에만 호출이 가능한가?

```
----------------------------------------------------------------------
아니다.
----------------------------------------------------------------------
함수 호출 코딩이 먼저 나오고 함수 선언이 나중에 나와도 된다.(이론적)
----------------------------------------------------------------------
웹브라우저는 html 파일 안의 html 소스 또는 자스소스를 읽어들일 때 아래와 같은 규칙으로 사용한다.
----------------------------------------------------------------------
    ----------------------------------------------------------------------
    <1> 먼저  정적함수 즉, function 함수명(~){~} 이렇게 생긴 함수를 먼저 골라 읽어 들인다.
    ----------------------------------------------------------------------
    <2> 맨 위에서 부터 html 소스 또는 자스소스를 차례대로 소스를 읽어 들여 실행한다. // 정적함수를 제외하고.
    ----------------------------------------------------------------------
----------------------------------------------------------------------
그렇지만 정적함수 즉, 이름있는 함수는 관용적으로 맨 위, head 태그 안에 선언하는 것이 좋다.
----------------------------------------------------------------------

<참고> 정적함수 선언은   function 함수명(~){~}  를 말한다. 
```

----
### `13:20~`

  - 점심시간.

---
---

## # 오후수업

---
### `14:30~`


### <36> 아래 자스코딩을 분석하면?

```javascript
----------------------------------------------
    if( isEmpty(pwd) ){
        alert("암호가 비어 있음. 입력바람");
        return;
    }
----------------------------------------------
```

```
----------------------------------------------------------------------
조건문인 if 문에서 if 오른쪽은 true 또는 false 를 리턴하는 [비교연산식] 또는 [논리연산식] 또는 [함수호출] 이 나온다.
----------------------------------------------------------------------
if( true 또는 false 를 리턴하는 [비교연산식] 또는 [논리연산식] 또는 [함수호출] ){
    실행구문;
}
----------------------------------------------------------------------
위 문제에서 함수호출 코딩인 isEmpty(pwd) 가 나왔으므로 isEmpty 함수의 리턴값은 true 또는 false 라고 볼 수 있다.
----------------------------------------------------------------------
<주의> 데이터가 나올 자리에 함수 호출 코딩이 나오면 함수 호출 후 반드시 리턴값이 있다.
```


### <37> 아래 자스코딩을 분석하면?

```
----------------------------------------------
document.write( "<table border=1 style='border-collapse:collapse' cellpadding=5 width=600>" );
----------------------------------------------
```

```
document객체의 write 메소드를 호출하고 html 소스인 table 태그를 리턴한다. 

document.write("문자열") => Document 객체의 write 메소드를 호출한 것이다.
                            변수 document 안에는 Document 객체가 이미 저장되어 있다.
                            괄호안의 문자열을 html 태그 소스 또는 html 문자열로 html 파일에 출력하란 말이다.
                            즉, <script>document.write("문자열")</script> 이 코드가 나오는 자리에 
                            문자열이 들어간다고 보면 된다.
```

#

## # JQuery
> JQuery 폴더 만든 후 [`JQuery_교재.txt`](https://github.com/SungWoo0315/new-study-repository/blob/main/JQuery/JQuery_%EA%B5%90%EC%9E%AC.txt) 생성.

### <1> JQuery 가 무엇입니까?

```
-------------------------------------------------------
● 공개적으로 개발되고 무료로 제공되는 [자바스크립트 사용자 정의 객체]의 이름이다. 
-------------------------------------------------------
<주의> 프로그램 언어의 한 종류가 아니다.  
<참고> 누구나 버그 수정, 성능향상 그리고 도움말 작성에 참여 할 수 있다.
<참고> http://plugins.jquery.com/ 에 광범위한 라이브러리(~.js)가 있으며, 꾸준히 업데이트되고 있고,
        누구나 버그 수정, 성능향상, 도움말 작성에 참여할 수 있다.
```

### <2> JQuery 의 장점.

```
-------------------------------------------------------
● 디자이너, 퍼블리셔, 개발자 모두가 사용하기 편하고 간단한 코드로 강력한 기능을 가진 메소드를 제공한다.
-------------------------------------------------------
● 대부분의 브라우저에서 JQuery 객체의 메소드를 호출하면 동일한 결과를 생산한다. (=cross browsing)
  즉, 브라우저에 따라 다른 코딩을 할 필요가 없다. 
  기존 자스코딩은 브라우저에 따라 다른 결과를 산출하는 어려움이 있었다.
-------------------------------------------------------
<참고> 한번의 코딩으로 모든 브라우저에서 같은 실행 결과를 산출하는 것을 크로스 브라우징(=cross browsing)이라고 한다.
        개발자가 제일 싫어하는 작업 중의 하나이다. 
-------------------------------------------------------
```

---
### `15:30~`

### <3> JQuery 객체의 메소드를 사용하기 위한 사전 작업은 무엇이 있습니까?

```
-------------------------------------------------------
● jquery-x.x.x.min.js 다운받고 HTML의 head 태그에 아래와 같이 수입하는 코드를 작성한다. x 는 임의의 버전 번호이다. 
-------------------------------------------------------
    <script src="jquery-1.6.2.min.js" type="text/javascript"></script>
-------------------------------------------------------
jquery-x.x.x.min.js 파일 안에는 JQuery 객체의 메위주를 리턴하는 jQuery 함수 또는 $ 함수가 존재하고,
JQuery 객체의 메소드나 속성변수가 선언되어 있다.
-------------------------------------------------------
```

### <4> JQuery 객체의 메위주를 얻으려면 어떻게 합니까?

```
-------------------------------------------------------
jQuery("선택자") 함수 또는 $("선택자") 함수를 호출하여 얻는다.
-------------------------------------------------------
jQuery("선택자") 함수 또는 $("선택자") 함수는 jquery-x.x.x.min.js 파일 안에 존재한다.
-------------------------------------------------------
<참고> JQuery 객체의 메위주를 얻어야 JQuery객체의 메소드를 호출할 수 있다.
```

### <5> JQuery 객체의 메위주를 얻고, 메소드를 호출하는 형식을 보여주세요.

```
---------------------------------
var xxx = $("선택자");          // $("선택자") 함수를 호출하여 얻은 JQuery 객체의 메위주를 변수 xxx 에 저장하기
---------------------------------
xxx.JQuery객체메소드명(~)        // JQuery 객체의 메소드 호출
---------------------------------
xxx.JQuery객체속성변수명         // JQuery 객체의 속성변수 호출
---------------------------------
```

### <6> jQuery("선택자") 함수 또는 $("선택자") 함수 호출 시 도대체 `선택자` 가 뭡니까? ★★★

```
-------------------------------------------------------
선택자(=selector)는 태그를 지칭 하는 방법론을 말한다.
-------------------------------------------------------
JQuery 객체는 태그를 관리하는 객체이므로 태그를 지칭하는 방법론이 여러가지가 있다. 
-------------------------------------------------------
<예> var obj = $("[name=uid]");
-------------------------------------------------------
    <1> 변수 obj 선언.
    <2> $ 함수를 호출하여 JQuery 객체의 메위주를 얻어 변수 obj 저장.
        이때 JQuery 객체는 name=uid 를 가진 태그를 관리한다. 
-------------------------------------------------------
<예> var obj = $(".pwd");
-------------------------------------------------------
    <1> 변수 obj 선언.
    <2> $ 함수를 호출하여 JQuery 객체의 메위주를 얻어 변수 obj 저장.
        이때 JQuery 객체는 class=pwd 를 가진 태그를 관리한다. 
-------------------------------------------------------
<예> var obj = $("#addr");
-------------------------------------------------------
    <1> 변수 obj 선언.
    <2> $ 함수를 호출하여 JQuery 객체의 메위주를 얻어 변수 obj 저장.
        이때 JQuery 객체는 id=addr 를 가진 태그를 관리한다. 
-------------------------------------------------------
<예> var obj = $(".family .relation");
-------------------------------------------------------
    <1> 변수 obj 선언.
    <2> $ 함수를 호출하여 JQuery 객체의 메위주를 얻어 변수 obj 저장.
        이때 JQuery 객체는 class=family 를 가진 태그 후손 즉, 내부에 
                          class=relation 가진 태그 관리한다.

                var obj = $(".relation"); // 이렇게되면 아래의 두가지를 관리하게 된다. 
                                          // 정확하게 하나만 관리하려면 부모를 잡고들어가야한다.
                -------------------------------------------------------
                <div class=family>
                    <span class=relation>얀센 접종자</span>
                </div> 
                <div class=employee>
                    <span class=relation>얀센 접종자2</span>
                </div> 
                -------------------------------------------------------
-------------------------------------------------------
<예> var obj = $("div");
-------------------------------------------------------
    <1> 변수 obj 선언.
    <2> $ 함수를 호출하여 JQuery 객체의 메위주를 얻어 변수 obj 저장.
        이때 JQuery 객체는 div 태그를 가진 태그를 관리한다. 
        만약 div 태그가 2개 이상이면, 정말 2개 이상을 관리한다. 
```

> 선택자는 문자열로 들어간다. 공백이 갖는 의미는 후손이다.  

> 후손은 자식 아래의 세대까지 의미. 자식은 바로 아래 세대까지만을 의미.

#

- JQuery 파일 작업.

  > JQuery 폴더 안에 html 폴더 만들고 `common_func.js` `common_date.js` 복사.

  > 강사님 `loginForm.html` 파일 JQuery 폴더 에 넣기.
  
  > `jquery-1.11.0.min.js` 파일 JQuery 폴더 에 넣기.

#

- loginForm.html 안에 수입 코드 작성.

  ```html
  <!-------------------------------------------------------->
  <!-- jquery-1.11.0.min.js 파일안의 자스코딩을 수입해서 script 태그안에 삽입하기.-->
  <!-- common_func.js 파일안의 자스코딩을 수입해서 script 태그안에 삽입하기.-->
  <!-------------------------------------------------------->
  <script src="jquery-1.11.0.min.js"></script>
  <script src="common_func.js"></script>
  ```

----
### `16:30~`

JQuery 쓰면,        
html 에서 자스코딩은 안나온다.       
<b>javascript 와 html 코딩의 완벽한 분리!!</b>     

유지보수를 위해서...!  

입력양식 중심 태그 주의해서 보기.  

- `loginForm.html` 작업.  

  body 태그안의 자스코딩 전부 삭제.  


버튼은 입력양식이긴 하나, 서버로 데이터가 가지 않는다.   
그래서 네임값을 주지 않아도 된다.  
class 값을 주는 이유는 JQuery 사용할때 찾기 위함.  


- 각 입력양식에 class 값 부여.  


- 로그인 버튼을 클릭하면 checkForm 함수가 호출되도록 하기

  ```javascript
  //--------------------------
  // 유저가 입력한 아이디와 암호의 유효성을 체크할 checkForm 함수 선언
  //--------------------------
  function checkForm(){

    // var uid = document.loginForm.uid.value; 아래와 같은 결과를 가진다 DOM 객체 사용.  
    var uid = $(".uid").val();
    var pwd = $(".pwd").val();

    alert(uid + " / " + pwd);
  }

  function start(){
    // ---------------------------------------------------
    // 로그인 버튼을 클릭하면 checkForm 함수가 호출되도록 하기
    // ---------------------------------------------------
    var obj = $(".login_btn");
    obj.click(
      function(){
        checkForm();
      }
    );
  }
  ```
  > `.` 왼쪽에는 JQuery 객체의 메위주. `.` 오른쪽은 JQuery 객체의 메소드.    

  > `click()`은 JQuery 객체의 메소드이다. 이벤트명 과는 다르다.  
  > JQuery는 이벤트 이름 자체가 메소드이다.    

  > JQuery 의 `val()` 메소드 사용. 입력한 value값 가져옴.  

----
### `17:30~`

정리시간.

----
### `18:30~` 

오라클 하루에 몇문제로 풀기.     

자스.    

자바.   

1~2주 JQuery        

CSS      

JSP/Spring      
  
