# 2021년 7월 30일 [55일차]

## # 오전수업
----
### `09:30~`
  
[`jquery_test_04.html`](https://github.com/SungWoo0315/new-study-repository/blob/main/JQuery/html/jquery_test_04.html)      

[`jquery_test_05.html`](https://github.com/SungWoo0315/new-study-repository/blob/main/JQuery/html/jquery_test_05.html)      

[`jquery_test_06.html`](https://github.com/SungWoo0315/new-study-repository/blob/main/JQuery/html/jquery_test_06.html)      

주석달고 풀이.      

어제 풀었던 내용과 비교해보기.     
  
프로그램 상단에 변수 선언하고 코딩.    

#

- `jquery_test_04.html` 풀이 주석.  

  ```javascript
    // --------------------------------------------------------
    // obj1 변수 선언하고 name='hobby_all' 을 가진 태그를 관리하는 JQuery 객체 생성해서 
    // 객체의 메위주를 obj1 에 저장하기.
    // --------------------------------------------------------
    var obj1 = $("[name='hobby_all']");
    // --------------------------------------------------------
    // obj2 변수 선언하고 name='hobby' 을 가진 태그를 관리하는 JQuery 객체 생성해서 
    // 객체의 메위주를 obj2 에 저장하기.
    // --------------------------------------------------------
    var obj2 = $("[name='hobby']");
    // --------------------------------------------------------
    // obj1 에 저장된 JQuery 객체가 관리하는 태그에 change 이벤트가 발생하면
    // 실행할 구문 설정하기.
    // --------------------------------------------------------
    obj1.change(
      function(){
        // ------------------------------------------------
        // 변수 isChecked 선언하고, change 이벤트가 발생한 것의 체크여부를 저장하기
        // <주의> 현재 this 는 change 이벤트가 발생한 태그를 지칭하는 선택자.  
        // ------------------------------------------------
        var isChecked = $(this).prop("checked");
        // ------------------------------------------------
        // 변수 isChecked 에 true 가 저장되어 있으면,
        // 즉, change 이벤트가 발생한 것이 체크가 되어 있으면 
        // 즉, 전체가 체크되어 있으면
        // ------------------------------------------------
        if( isChecked==true ){
          // 변수 obj2 에 저장된 JQuery 객체가 관리하는 태그에
          // 체크를 넣기.  
          obj2.prop("checked", true);
        }
        // ------------------------------------------------
        // 변수 isChecked 에 false 가 저장되어 있으면,
        // 즉, change 이벤트가 발생한 것이 체크가 안되어 있으면 
        // ------------------------------------------------
        else{
          // 변수 obj2 에 저장된 JQuery 객체가 관리하는 태그에
          // 체크를 빼기.  
          obj2.prop("checked", false);
        }

        // -------------------------------------
        // 위 코드를 줄여서 만든 식.
        // <참고> 위 모든 코드는 이것으로 대체가 가능하다.
        // -------------------------------------
        // obj2.prop( "checked", $(this).prop("checked") )

      }
    );
    // --------------------------------------------------------
    // obj2 에 저장된 JQuery 객체가 관리하는 태그에 change 이벤트가 발생하면
    // 실행할 구문 설정하기.
    // 즉, 독서, 운동, 여행, 수집 으로 표현되는 체크박스에 체크가 들어가거나 체크가 빠지면
    // 실행할 구문 설정하기.
    // --------------------------------------------------------
    obj2.change(
      function(){
        
        // ------------------------------------------
        // obj1 에 저장된 JQuery 객체가 관리하는 태그에
        // obj2.filter(":checked").length==obj2.length 의 실행결과에 따라
        // 체크를 넣어주거나, 빼주기
        // 즉, obj2.filter(":checked").length==obj2.length 가 true 면 체크가 들어가는것이고
        //     obj2.filter(":checked").length==obj2.length 가 false 면 체크가 빠지는것이다. 
        // obj2.filter(":checked").length==obj2.length 의 의미
        // 체크된 것의 개수와 존재하는 것의 개수가 같으면 true, 다르면 false
        // 체크된 것의 개수와 존재하는 것의 개수가 같다라는 말은 모두 체크되어 있다는 말이다.
        // ------------------------------------------
        obj1.prop( "checked", obj2.filter(":checked").length==obj2.length )

      }
    );
  // --------------------------------------------------------
  ```
  > <주의> 현재 this 는 change 이벤트가 발생한 태그를 지칭하는 선택자.     

  > 이벤트 처리,    

  > length 를 사용하여 개수가 늘어나도 적용되도록 하는것이다.    

  > `filter()` 는 후손이 아니라 JQuery 객체가 관리하는것중에 찾는것.    
  > `find()` 와 혼동하지 말것. `find()` 는 후손중에 찾는것.     

  > 비교연산자 `==` 의 결과값은 true 아니면 false 이다.  

#

----
### `10:30~`

- `jquery_test_05.html` 풀이 주석.  

  ```javascript
    // ******************************************************
    // 변수 obj 선언하고 class="dogList" 를 가진 태그를 관리하는 
    // JQuery 객체를 생성하고, 객체의 메위주를 obj 에 저장하기.
    // ******************************************************
    var tableObj = $(".dogList");
    var trObj = tableObj.find("tr");

    // ******************************************************
    // trObj 에 저장된 JQuery 객체가 관리하는 태그에 차례대로 접근해서
    // function(i){~} 라는 익명함수 안의 코딩을 실행하기.
    // ******************************************************
    trObj.each( 
      function(i){
        // 각 tr 태그의 첫째 자식으로 <th>번호 또는 <td>일련번호 삽입하기.  
        // --------------------------------------------- 
        // 변수 thisObj 선언하고 i 번째 tr 태그를 관리하는   
        // JQuery 객체를 생성하고, 객체의 메위주 저장하기  
        // --------------------------------------------- 
        var thisObj = $(this);
        // --------------------------------------------- 
        // 익명함수의 매개변수 i 가 0이면  
        // --------------------------------------------- 
        if(i==0){
          // i 번째 tr 태그 안의 첫째 자식으로 "<th bgcolor=gray>번호" 를 삽입하기.  
          thisObj.prepend("<th bgcolor=gray>번호")
        }
        // --------------------------------------------- 
        // 익명함수의 매개변수 i 가 0 이 아니면
        // --------------------------------------------- 
        else{
          // i 번째 tr 태그 안의 첫째 자식으로    <th>i변수값    을 삽입하기.  
          thisObj.prepend( "<th>" + i )
        }


    });
  ```
  > 객체의 메위주가 필요한 이유는 메서드 호출하려고!  
  > 객체의 메위주를 얻어야 메서드를 호출 할 수 있다.  

  > tr 태그의 첫번째 자식으로 들어가야 하므로, `prepend()` 메서드 사용하여야 한다.  

  > `each()` 메서드 형태를 암기해두어야 한다.  

  > `var trObj = obj.find("tr");` 는 tr 태그 6개를 관리하는 JQuery 객체 메위주.  

  > `var thisObj = $(this);` 의 `this`는 i 번째 tr 을 지칭하러 들어간다.  

  > 익명함수 `function(i)` 의 i 는 자동으로 0 부터 증가하면서 trObj 개수 만큼 들어가고 0 은 첫번째 tr 태그의 인덱스를 잡는다.  

----
### `11:30~`

- `jquery_test_05.html` 역순번호 풀이 주석.  

  ```javascript
    // %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
    // i 값안쓰고 정순번호.  
    // %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

    var no = 1;

    trObj.each( 
        function(i){
          var thisObj = $(this);
          if(i==0){
            thisObj.prepend("<th>번호")
          }
          else{
            thisObj.prepend("<td>" + no++ )
          }
        }
    );
    // %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%



    // %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
    // i 값 안쓰고 역순번호.
    // %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

    var no = trObj.length-1;

    trObj.each( 
        function(i){
          var thisObj = $(this);
          if(i==0){
            thisObj.prepend("<th>번호")
          }
          else{
            thisObj.prepend("<td>" +  no-- )
          }
        }
    );
    // %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

    
    // %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
    // 내가 해본 i값안쓰고 역순 번호.
    // %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
    var trLen = trObj.length
    var no = 1;

    trObj.each( 
        function(i){
          var thisObj = $(this);
          if(i==0){
            thisObj.prepend("<th>번호")
          }
          else{
            thisObj.prepend("<td>" +  (trLen-no++) )
          }
        }
    );
    // %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%




    // ******************************************************
    // 역순번호
    // ******************************************************
    var trLen = trObj.length

    trObj.each( 
      function(i){
        // 각 tr 태그의 첫째 자식으로 <th>번호 또는 <td>일련번호 삽입하기.  
        // --------------------------------------------- 
        // 변수 thisObj 선언하고 i 번째 tr 태그를 관리하는   
        // JQuery 객체를 생성하고, 객체의 메위주 저장하기  
        // --------------------------------------------- 
        var thisObj = $(this);
        // --------------------------------------------- 
        // 익명함수의 매개변수 i 가 0이면  
        // --------------------------------------------- 
        if(trLen-i == trLen){
          // i 번째 tr 태그 안의 첫째 자식으로 "<th bgcolor=gray>번호" 를 삽입하기.  
          thisObj.prepend("<th bgcolor=gray>번호")
        }
        // --------------------------------------------- 
        // 익명함수의 매개변수 i 가 0 이 아니면
        // --------------------------------------------- 
        else{
          // i 번째 tr 태그 안의 첫째 자식으로    <th>i변수값    을 삽입하기.  
          thisObj.prepend( "<th>" + (trLen - i) )
        }


    });

  ```
  > 다양한 방식으로 역순번호를 만들어 볼 수 있다.  

  > 각각의 차이 코딩 해석해보기.  

  #

- `jquery_test_06.html` 풀이 주석.  

  ```javascript
    // -----------------------------------
    // class="reverse" 을 가진 태그를 클릭하면 function(i){~} 라는 익명함수 호출하기.
    // -----------------------------------
    $(".reverse").click(
      function(){

        // -----------------------------------
        // 변수 hobbyObj 선언하고 name="hobby" 를 가진 태그를 관리하는 
        // JQuery 객체를 생성하고, 객체의 메위주를 hobbyObj 에 저장하기.
        // -----------------------------------
        var hobbyObj = $("[name='hobby']");
        // -----------------------------------
        // hobbyObj 에 저장된 JQuery 객체가 관리하는 태그에 차례대로 접근해서
        // function(i){~} 라는 익명함수 안의 코딩을 실행하기
        // -----------------------------------
        hobbyObj.each(
          function(i){
            // --------------------------------------
            // 변수 isChecked 선언하고 i번째 태그의 체크여부를 저장하기.
            // <주의> 현재 this 는 i번째 태그를 지칭하는 선택자
            // --------------------------------------
            var isChecked = $(this).prop("checked");
            // --------------------------------------
            // 만약 변수 isChecked 에 true 가 저장되어 있으면
            // 즉, 체크가 되어 있으면
            // --------------------------------------
            if( isChecked==true){
              // 체크를 풀기.
              $(this).prop("checked", false);
            }
            // --------------------------------------
            // 만약 변수 isChecked 에 false 가 저장되어 있으면
            // 즉, 체크가 안되어 있으면
            // --------------------------------------
            else{
              // 체크를 넣기
              $(this).prop("checked", true);
            }

          }

        );


        // %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
        // 조건문을 쓰지않고 each() 만으로 가능하도록 작성.  
        // %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
        var hobbyObj = $("[name='hobby']");
        hobbyObj.each( function(i){
            var thisObj = $(this);

            thisObj.prop("checked", !thisObj.prop("checked"))
            
        });
        // %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
        
  ```
  > each() 메서드를 사용하여서 만든 코딩. 

  > each 구문 안에서 `this` 는 순서중에 현재 `each가 잡은 체크박스` 객체의 메위주 이다.  

  > `jquery_test_06.html` 파일 안에 다른 풀이 코딩들이 더 있다. 참고하기.  

----
### `12:30~`

- `jquery_test_06.html` 풀이 주석 이어서.   

  ```javascript
    // %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
    // each() 쓰지 않고. 가능하도록 작성. // 반복문 쓰지 않고. 
    // %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
    var hobbyObj = $("[name='hobby']");

    var checkedObj = hobbyObj.filter(":checked");
    var not_checkedObj = hobbyObj.filter(":not(:checked)");

    checkedObj.prop("checked", false);
    not_checkedObj.prop("checked", true);
    // %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
  ```
  > 반복문 쓰지않고 가능하도록 작성.

#

- `jquery_test_06.html` 오답 분석하기.

  ```javascript
    // %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
    // each() 쓰지 않고. 가능하도록 작성. // 오답. 찾기. 
    // %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
    var hobbyObj = $("[name='hobby']");

    hobbyObj.filter(":checked").prop("checked", false);
    hobbyObj.filter(":not(:checked)").prop("checked", true);

  
    // %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
  ```
  > 체크된것을 골라내어 `false` 로 바꿔주자마자,  
  > 바로 아래 [체크안된것]을 골라내는 `":not(:checked)"` 에 걸려서 `true` 로 전환된다.    

  > 바로 체크된것을 골래내어 체크를 풀어주자 마자 다음 체크안된것에 걸려버리는 것이다.  

  > 반대로 정답 코딩에서 오답처럼 되지 않고 올바르게 작동하는 이유는  
  > 변수 `checkedObj` 와 `not_checkedObj` 를 각각 선언하여  
  > 체크 된것과 체크 안된것을 따로 구분해서 담아주고,  
  > 이것을 따로 불러와서 변수 `checkedObj` 와 `not_checkedObj` 에 담겨 있는 것들만 각각 풀어주고 체크해주고를 실행하여서 서로 영향이 가지 않게 하여 오답이 나지 않는것이다.  

   
----
### `13:20~`

  - 점심시간.

---
---

## # 오후수업

---
### `14:30~`

- 오전 마지막에 했던 오답 이미지.

  > ![변수선언 유무.오답풀이.](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210730%201441_%EB%B3%80%EC%88%98%EC%84%A0%EC%96%B8%20%EC%9C%A0%EB%AC%B4_%EC%98%A4%EB%8B%B5%ED%92%80%EC%9D%B4_.png)  

  > 변수를 이용하지 않으면 오류가 난다.

#

- 오전 마지막에 했던 정답 이미지.

  > ![변수선언.정답풀이](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210730%201443_%EB%B3%80%EC%88%98%EC%84%A0%EC%96%B8_%EC%A0%95%EB%8B%B5%ED%92%80%EC%9D%B4.png)  

  > 변수를 이용하여 서로 영향가지 않게 하여 오류가 나지 않는다.  

#

- `jquery_test_06.html` 주석달기.  

  ```javascript
  	// %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
    // each() 쓰지 않고. 가능하도록 작성. // 반복문 쓰지 않고. 
    // %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
    // --------------------------------------
    // 변수 hobbyObj 선언하고 name="hobby" 를 가진 태그를 관리하는
    // JQuery 객체를 생성하고 객체의 메위주를 obj 에 저장하기.
    // --------------------------------------
    var hobbyObj = $("[name='hobby']");
    // --------------------------------------
    // 변수 checkedObj 선언하고 name="hobby" 를 가진 태그 중에
    // 체크된 것을 관리하는 JQuery 객체를 생성해서 메위주를 변수 checkedObj 에 저장하기.
    // --------------------------------------
    var checkedObj = hobbyObj.filter(":checked");
    // --------------------------------------
    // 변수 not_checkedObj 선언하고 name="hobby" 를 가진 태그 중에
    // 체크안된 것을 관리하는 JQuery 객체를 생성해서 메위주를 변수 checkedObj 에 저장하기.
    // --------------------------------------
    var not_checkedObj = hobbyObj.filter(":not(:checked)");
    // --------------------------------------
    // 변수 checkedObj 에 저장된 JQuery 객체가 관리하는 것의 체크를 풀기
    // --------------------------------------
    checkedObj.prop("checked", false);
    // --------------------------------------
    // 변수 not_checkedObj 에 저장된 JQuery 객체가 관리하는 것의 체크를 넣기
    // --------------------------------------
    not_checkedObj.prop("checked", true);
    // %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%



    // %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
    // each() 쓰지 않고. 가능하도록 작성. // 오답. 찾기. 
    // %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
    // --------------------------------------
    // 변수 hobbyObj 선언하고 name="hobby" 를 가진 태그를 관리하는
    // JQuery 객체를 생성하고 객체의 메위주를 obj 에 저장하기.
    // --------------------------------------
    var hobbyObj = $("[name='hobby']");
    // --------------------------------------
    // name="hobby" 를 가진 태그 중에 체크된것의 체크를 풀어라
    // --------------------------------------
    hobbyObj.filter(":checked").prop("checked", false);
    // --------------------------------------
    // name="hobby" 를 가진 태그 중에 체크안된것의 체크를 해라
    // 위 코드에 체크가 풀린 것도 걸려든다.   
    // --------------------------------------
    hobbyObj.filter(":not(:checked)").prop("checked", true);			
    // %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% 
  ```

---
### `15:30~`, `16:30~`  

## # `jquery_test_07.html` 예제 파일 코딩.  [검색]  

```javascript
  // ----------------------------------------------------
  // 변수 tableObj 선언하고 class=sungjuk 을 가진 태그를 관리하는 
  // JQuery 객체 생성하고 메위주를 저장하기
  // ----------------------------------------------------
  var tableObj = $(".sungjuk");
  // ----------------------------------------------------
  // thead 태그 안의 tr 의 배경색을 lightblue 로 하기
  // JQuery 객체의 css 라는 메서드를 호출해서 CSS 차원에서 배경색을 lightblue 로 하기.  
  // ----------------------------------------------------
  // tableObj.find("thead tr").attr("bgcolor", "lightblue"); // attr() 메서드 사용해서 색상부여.
  tableObj.find("thead tr").css("background-color", "lightblue");
  // ----------------------------------------------------
  // tbody 태그 안의 짝수 tr 의 배경색을 lightgray 로 하기
  // JQuery 객체의 css 라는 메서드를 호출해서 CSS 차원에서 배경색을 lightgray 로 하기.  
  // ----------------------------------------------------
  tableObj.find("tbody tr:odd").css("background-color", "lightgray");
  // ----------------------------------------------------
  // tbody 태그 안의 홀수 tr 의 배경색을 white 로 하기
  // ----------------------------------------------------
  tableObj.find("tbody tr:even").css("background-color", "white");


  // ----------------------------------------------------
  // class="keyword1" 가진 태그에 click 이벤트가 일어나면
  // 키워드를 가지고 검색한 후 검색된 행만 보여주고 검색되지 않는 행은 감추는 코딩 설정하기
  // ----------------------------------------------------
  $(".search").click(function(){

    // -------------------------------------
    // 입력된 키워드값을 얻어내어 keyword1 변수에 저장
    // -------------------------------------
    var keyword1 = $(".keyword1").val();
    // -------------------------------------
    // 입력된 키워드값이 공백으로 구성되거나 또는 비어 있으면 경고하고 키워드 비우고 검색 중지
    // -------------------------------------
    if(keyword1.split(" ").join("")==""){

      alert("검색 데이터가 없습니다.");
      $(".keyword1").val("");
      $(".keyword1").focus();
      return;
    }
    // -------------------------------------
    // keyword1 변수에 저장된 데이터의 앞뒤 공백을 제거하기.
    // JQuery 라이브러리가 제공하는 $ 변수에는 태그를 관리하지 않는 JQuery 객체의 메위주가 저장되어 있다.
    // JQuery 객체의 trim("문자열") 메서드를 호출하면 앞뒤 공백을 제거하고 문자를 리턴한다.    
    // -------------------------------------
    // keyword1 = keyword1.trim();
    keyword1 = $.trim(keyword1);
    // -------------------------------------
    // 입력된 키워드값을 소문자로 바꿈. 
    // 영어는 비교할 때 소문자 또는 대문자로 모두 통일해서 바꾼 후 비교해야함.
    // -------------------------------------
    keyword1 = keyword1.toLowerCase();
    // -------------------------------------
    // class=sungjuk 가진 태그 후손의 tbody 태그 후손의 
    // tr 태그(=현재 17개 tr)를 관리하는 JQuery 객체의 메위주를 변수 trsObj 에 저장하기
    // -------------------------------------
    var trsObj = tableObj.find('tbody tr');
    // -------------------------------------
    // table 태그 후손 중 tbody 후손 중 모든 tr 을 감추기
    // -------------------------------------
    trsObj.hide();
    // -------------------------------------
    // 검색 대상이되는 tr 태그들을 차례대로 접근해서
    // tr 내부에 검색 키워드를 가지고 있으면 화면에 보이게 하기.  
    // -------------------------------------
    trsObj.each( function(){
      // i번째 tr 태그를 관리하는 JQuery 객체의 메위주를 얻어서 변수 trsObj 에 저장하기.
      var trObj = $(this);
      // i번째 tr 태그 내부의 1번째 td 태그가 안고 있는 문자열 얻고 소문자로 바꾸어 변수 text1 에 저장하기
      var text1 = trObj.find("td:eq(0)").text().toLowerCase();
      // i번째 tr 태그 내부의 2번째 td 태그가 안고 있는 문자열 얻고 소문자로 바꾸어 변수 text2 에 저장하기
      var text2 = trObj.find("td:eq(1)").text().toLowerCase();
      // i번째 tr 태그 내부의 3번째 td 태그가 안고 있는 문자열 얻고 소문자로 바꾸어 변수 text3 에 저장하기
      var text3 = trObj.find("td:eq(2)").text().toLowerCase();
      
      // alert(text1 + " / " + text2 + " / " + text3);	// 문자열 리턴 확인 테스트용. 

      // 1번째 또는 2번째 또는 3번째 td 태그가 안고 문자열에 keyword1 변수 안의 데이터가 있으면  
      if( text1.indexOf(keyword1)>=0 || text2.indexOf(keyword1)>=0 || text3.indexOf(keyword1)>=0 ){
        
        
        trObj.show();
      }

    });

  });



  // 모두검색 눌렀을 때 모두 나오게 하는 코딩.  
  $(".allSearch").click(function(){

  var tableObj = $(".sungjuk");

  tableObj.find("tr").show();

  });
```
> thead 태그 안의 tr 의 배경색을 lightblue 로 하기

> `css()` 메서드가 있다.

> 짝수 tr 에 배경색 지정하기.  

> 검색버튼 누르면, 키워드 안의 입력된 데이터를 가지고 각 행을 찾아야 한다.  

> 키워드 비워있거나 공백이면 찾지 않아야 한다.  

> 검색버튼 누르면 실행되는 이벤트 설정.  

> 검색창에 입력한 데이터 얻어오기.  

> 공백이나 비어 있을 때 경고하기.  

> `trim()` 으로  공백 제거를 하면 익스플로러 버전에 따라서 호환이 안될수도 있다.  

> JQuery 객체 중에 앞뒤 공백 제거하는 메서드가 있다.  

> JQuery 객체 에 `$` 변수가 있다. 여기에는 태그를 관리하는 객체는 없다.   


----
### `17:30~`

## # JAVA 진도.  

```
------------------------------------------------------------------
기본자료형|클래스명|인터페이스명
------------------------------------------------------------------
    변수안에 저장되는 데이터의 유형(=자료형=data type)을 말한다.  
    ----------------------------------------------------------
    <예> ~ int xxx;       =>    xxx 변수에는 정수형 데이터가 저장된다.  
    <예> ~ double xxx;    =>    xxx 변수에는 실수형 데이터가 저장된다.  
    <예> ~ boolean xxx;   =>    xxx 변수에는 true 또는 false 가 저장된다.  
    <예> ~ char xxx;      =>    xxx 변수에는 문자 1개가 저장된다.  
    ----------------------------------------------------------
    <예> ~ String xxx;    =>    xxx 변수에는 문자열을 관리하는 String 객체의 메위주가 저장된다.  
                          ------------------------------------------------------
                                ~ String xxx = "한승태";
                                위 코드는 아래 코드와 거의 동일하다고 봐야한다.
                                절대 xxx 에 "한승태" 라는 문자가 저장된게 아니다.
                                ~ String xxx = new String("한승태");
                          ------------------------------------------------------
    <예> ~ ArrayList xxx; =>    xxx 변수에는 ArrayList 클래스가 객체화 됐을 때 객체의 메위주가 저장될 예정이다.  
    <예> ~ HashMap xxx;   =>    xxx 변수에는 HashMap 클래스가 객체화 됐을 때 객체의 메위주가 저장될 예정이다.  
    ----------------------------------------------------------
    <예> ~ List xxx;      =>    xxx 변수에는 List 라는 인터페이스를 구현한 클래스 중에 하나가 객체화 됐을 때 
                                객체의 메위주가 저장될 예정이다.  
                                List 라는 인터페이스를 구현한 클래스들은 ArrayList, LinkedList, Vector, Stack 가 있다.  
                                결국, xxx 변수에는 ArrayList, LinkedList, Vector, Stack 중에 하나가 객체화 했을 때 
                                메위주가 들어갈 예정이다. 
    <예> ~ Map xxx;       =>    xxx 변수에는 Map 라는 인터페이스를 구현한 클래스 중에 하나가 객체화 됐을 때 
                                객체의 메위주가 저장될 예정이다.  
                                Map 라는 인터페이스를 구현한 클래스들은 HashMap, Hashtable, TreeMap, LindedHashMap,
                                Properties 가 있다.  
                                결국, xxx 변수에는 HashMap, Hashtable, TreeMap, LindedHashMap, Properties 중에 하나가 객체화 했을 때 
                                메위주가 들어갈 예정이다. 
```
> String 객체의 메위주 조심하기!    

> 기본형이 아닌 이상 `참조형`이다.  `String` 주의!  

> 자바는 `자료형`에 대한 엄격성이 있다.    

> `인터페이스`는 객체화 될 수 없다. 객체화 목적이 아니라서 객체화가 안된다.      
> 다른 클라스가 구현받아서 사용할 목적이다.    

> 변수옆에 인터페이스명이 붙으면?  

> 기본형/클래스명/인터페이스명    

> 암기를 해야한다.    

> `인터페이스` 공부하기.   

> 스프링에서는 `인터페이스 자료형`이 자주 쓰인다. (유지보수성 증가 위해.)    
> 인터페이스 자료형을 스프링에서 쓰게되면, `코딩의 연쇄 수정`을 막아준다.    

----
### `18:30~`
