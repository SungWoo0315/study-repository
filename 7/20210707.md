# 2021년 7월 7일 [40일차]

## # 오전수업
----
### `09:30~`

<복습>   

- 객체생성의 목적 => 메소드 호출      

- 각 객체의 메소드 관련을 외우기.    


1. 기능  
2. 리턴값 ☆☆☆☆  
3. 매개변수로 넘기는 데이터  


<각각의 내용 정리해서 적어보기>    

- DOM 객체  

- BOM 객체  

- 자바스크립트 내장 객체 (Array 객체)  

- JQuery  

자바계통에서는 spring프레임워크를 많이 쓴다. 유지보수를 위해   

#### Array 객체

#

### <16> 자바스크립트 내장 객체의 종류와 메소드/속성변수 정의

어제 이어서 나감. ▶Array 객체의 메소드 종류  

```
-----------------
Array 객체
-----------------
    ▶같은 유형 또는 다르유형의 다수 데이터를 [순차적]으로 저장하여 관리하는 내장 객체이다.
    ▶저장되는 데이터는 [배열변수]라는 곳에 저장된다.

    ▶Array 객체 생성하고 데이터 저장하는 방법
            -----------------
            <방법1>
            -----------------
                var 객참변수 = new Array( 데이터1, ~, 데이터n ); // 잘 안쓰는 방법
                객참변수 = 객체 참조 변수
            -----------------
            <방법2> ★★★
            -----------------
                var 변수명 = new Array( );
                변수명.push(데이터1);
                변수명.push(데이터2);
                ~
                변수명.push(데이터n);
            -----------------
            <방법3>
            -----------------
                var 변수명 = new Array( );
                변수명[0] = 데이터1;
                변수명[1] = 데이터2;
                ~
                변수명[n] = 데이터n;
            -----------------
            <방법4> ★★★
            -----------------
                var 변수명 = [데이터1, ~, 데이터n];
            -----------------
            <방법5> ★★★
            -----------------
                var 변수명 = [];
                변수명.push(데이터1);
                변수명.push(데이터2);
                ~
                변수명.push(데이터n);

    ▶Array 객체의 메소드 종류
            ------------------------------------------------------------------------------------------
            push( 데이터 )     => Array 객체안에 마지막에 배열 변수 만들고 그 배열 변수에 데이터 저장하기
            ------------------------------------------------------------------------------------------
            join( "연결문자" ) => Array 객체가 소유한 모든 저장 데이터를 연결문자로 연결하여 [문자열]로 [리턴]
            ------------------------------------------------------------------------------------------
                var xxx = [1,2,3]; var yyy = xxx.join(" "); 
            ------------------------------------------------------------------------------------------
            reverse( )        => 배열변수 안의 데이터의 순서를 거꾸로 뒤집음.  
            ------------------------------------------------------------------------------------------
            pop( )            => 마지막 배열변수안의 데이터를 리턴하고 마지막 배열변수를 제거함. 오려내기.  
            ------------------------------------------------------------------------------------------
            concat( 다른Array객체 메위주 ) => 현 Array 객체와 다른 Array 객체의 배열변수를 복사해서 새로운 Array 객체안에 
                                             저장하고 새로운 Array 객체를 [리턴]함. 그림참조. [20210707.md] 참고
            ------------------------------------------------------------------------------------------
            sort( )           => 저장된 데이터를 [문자열] 취급해 오름차순 정렬.  
                                 숫자가 아닌, 문자가 저장되어 있을때 정렬 시 사용하는 메소드다.  
                                ----------------------------------------------------------------------
                                var names = ["김소희","성이현","변희원"]; kors.sort( ); 
                                                                       // 정렬순서는 "김소희", "변희원", "성이현" 이다.
                                ----------------------------------------------------------------------
                                 var kors = [3, 20, 10]; kors.sort( ); // 정렬순서는 10, 20, 3 이다.
                                ----------------------------------------------------------------------
            ------------------------------------------------------------------------------------------
            sort( function(left,right){return left-right;} )
            ------------------------------------------------------------------------------------------
                    ▶저장된 숫자 데이터를 오름차순 정렬
                    ▶만약, 문자열을 정렬하고 싶다면 위에 있는 sort( ) 를 써라
                    ▶<예>var kors = [3,20,10]; kors.sort(function(left,right){return left-right;}) ;   
                                // 정렬순서는 3 10 20 이다.
            ------------------------------------------------------------------------------------------
            sort( function(left,right){return right-left;} )
            ------------------------------------------------------------------------------------------
                    ▶저장된 숫자 데이터를 내림차순 정렬
                    ▶만약, 문자열을 정렬하고 싶다면 위에 있는 sort( ) 를 써라
                    ▶<예>var kors = [3,20,10]; kors.sort(function(left,right){return right-left;}) ;   
                                // 정렬순서는 20 10 3 이다.
            ------------------------------------------------------------------------------------------
            slice( 시작인덱스번호, 끝순서번호 )
            ------------------------------------------------------------------------------------------

            ------------------------------------------------------------------------------------------
            splice( 시작인덱스번호, 개수 )
            ------------------------------------------------------------------------------------------

```

----
### `10:30~`

- Array 객체 이미지. (메모리 상상도)    

  ![Array 객체 이미지](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210707%201008_01_Array%20%EA%B0%9D%EC%B2%B4%20%EC%83%9D%EC%83%81%EB%8F%841.png)    

  ![Array 객체 이미지2](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210707%201015_01_Array%20%EA%B0%9D%EC%B2%B4%20%EC%83%9D%EC%83%81%EB%8F%842.png)    

  순차적으로 저장하는 이유 => 반복문 써서 꺼내려고   

  concat( );
  ![concat 설명](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210707%201045_01_Array%20%EA%B0%9D%EC%B2%B4%20%EC%83%9D%EC%83%81%EB%8F%84_concat.jpg)   

  sort 숫자 익명함수 사용 설명.  
  ![sort 숫자 익명함수](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210707%201109_01_Array%20%EA%B0%9D%EC%B2%B4%20%EC%83%81%EC%83%81%EB%8F%84_sort_number.jpg)  


★ push 리턴값은 [없다]  

★ join 리턴값은 [문자열]      

★ reverse 리턴값은 [없다]    

★ pop 리턴값은 [마지막 배열변수안의 데이터]     

★ concat 리턴값 [새로운 Array 객체의 메위주]  

★ sort 리턴값 [없다]    
    => 배열변수에 숫자보다는 문자가 들어있을때 `sort()` 사용하는것이 좋다. 리턴값이 문자열이라서 숫자는 올바르게 정렬이 잘 안된다.   
    숫자 정렬은 매개변수를 익명함수로 넣어서 한다.    
        sort( function(left,right){return left-right;} ) 오름  
        sort( function(left,right){return right-left;} ) 내림  
    잠시 문자취급해서 정렬하지, 문자로 바꾸진 않음.       

    내림차순은 sort() 후 reverse() 사용하면 내림차순이 된다.    

----
### `11:30~`

자바공부할때도 메소드위치주소값, 리턴값, 유의해가면서 공부.  

```
------------------------------------------------------------------------------------------
slice( 시작인덱스번호, 끝인덱스번호 )
------------------------------------------------------------------------------------------
        ▶시작인덱스번호 부터 [끝인덱스번호 이전]까지 [복사]해서 [새로운 Array 객체]로 리턴  
------------------------------------------------------------------------------------------
splice( 시작인덱스번호, 개수 )
------------------------------------------------------------------------------------------
        ▶[시작 인덱스번호] 부터 [개수] 만큼 [오려내서] [새로운 Array 객체]로 리턴.  

▶Array 객체 속성변수
------------------------------------------------------------------------------------------
length  =>  배열변수의 개수 저장되어 있음
------------------------------------------------------------------------------------------ 
```


- slice 설명 이미지    
  ![slice](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210707%201139_Array_slice_.jpg)      

- splice 설명 이미지  
  ![splice](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210707%201142_Array_splice_.jpg)    


★ slice 의 리턴값 [Array 객체]

```
Tip  
오라클은 인덱스번호 없고 순서번호이다.    
나머지 자바 자스 에서 인덱스 번호는 [0]부터 시작된다.    
```

```
reverse 리턴값 x
push 리턴값 x
join 리턴값 문자열
pop 리턴값 마지막 배열변수안의 데이터
sort 리턴값 x
concat 리턴값 새로운 array 객체
splice 리턴값 새로운 array 객체
slice 리턴값 새로운 array 객체
```

```
**********************************
Date 객체
**********************************
    ●년, 월, 일, 요일, 시, 분, 초 를 관리하는 객체이다. 
    ---------------------------------------------------
    ●객체 생성, 메소드 호출, 속성변수 호출 방법
    ---------------------------------------------------
        ------------------------------------------------------------
        객체생성 => var 변수명 = new Date( 년, 월-1, 일, 시, 분, 초 );
        ------------------------------------------------------------
        메소드호출 => 객참변수.메소드(~)
        ------------------------------------------------------------
        <주의>년, 월-1, 일, 시, 분, 초 를 세팅하지 않으면 현재 시스템 날짜가 세팅된다.
        <주의>년, 월-1, 일 만 세팅하면 시,분,초 는 0,0,0 이 세팅된다.
        <주의> Date 객체는 월을 0~11 까지 숫자로 관리하므로 항상 세팅 시 월은 -1 한 다음 삽입한다.
    -----------------------------------------
    ●Date 객체의 메소드
    -----------------------------------------
        getFullYear( ) => Date 객체에 세팅된 연도를 리턴.  
        getMonth( )    => Date 객체에 세팅된 월(0~11)를 리턴.
        getDate( )     => Date 객체에 세팅된 일(1~31)를 리턴. 
        getDay( )      => Date 객체에 세팅된 요일( 0(일) ~ 6(토) )을 리턴.
        getHours( )    => Date 객체에 세팅된 시간(0~23)을 리턴.  ※ 마지막에 s 붙는것 주의!!!!!
        getMinutes( )  => Date 객체에 세팅된 분(0~59)을 리턴.    ※ 마지막에 s 붙는것 주의!!!!!
        getSeconds( )  => Date 객체에 세팅된 초(0~59)을 리턴.    ※ 마지막에 s 붙는것 주의!!!!!
        getTime( )     => [1970년1월1일0시0분0초] ~ [Date 객체의 기억날짜] 까지의 차이를 초*1000 으로 리턴.

        setFullYear(년도) => Date 객체에 연도를 세팅한다
		setMonth(월-1 )   => Date 객체에 월을 세팅한다
		setDate(일 )      => Date 객체에 일을 세팅한다
		setHours(시 )     => Date 객체에 시를 세팅한다
		setMinutes(분 )   => Date 객체에 분을 세팅한다
		setSeconds(초 )   => Date 객체에 초를 세팅한다
    -----------------------------------------
    ●Date 객체의 예제
    -----------------------------------------
        현재 날짜의 년월일을 구해 변수 year, month, date 에 저장하기 

        var today = new Date( );
        var year = today.getFullYear();
        var month = today.getMonth()+1;
        var date = today.getDate();        
```

Date 객체는 월을 0부터 11까지 관리한다.    
ex) 3이 출력되면 4월로 인식하여야한다.    

`getHours( )`    
`getMinutes( )`    
`getSeconds( )`     
**※ 마지막에 s 붙는것 주의!!!!!**  

★ Date 객체의 메소드 리턴값 [정수]

----
### `12:30~`

- getTime( ) 메소드 설명  : 두 날짜의 차이를 구할때 사용  
  ![getTime]()  


★ getTime 메소드 리턴값 [정수(초)]

```
<<참고>>

60초 1분
60x60초 1시간
60x60x24 60x24시간(1일)

~초 / (60x60x24)
```

★ set~~~ 메소드 리턴값 [없음] 단순히 셋팅만 한다.  

```
-----------------------------------------
●Date 객체의 예제
-----------------------------------------
    현재 날짜의 년월일을 구해 변수 year, month, date 에 저장하기 

    var today = new Date( );
    var year = today.getFullYear();
    var month = today.getMonth()+1;
    var date = today.getDate();
```



----
### `13:20~`

  - 점심시간.

---
---

## # 오후수업

---
### `14:30~`










---
### `15:30~`









----
### `16:30~`








----
### `17:30~`








----
### `18:30~`
