# 2021년 9월 24일 [89일차]

## # 오전수업
----
### `09:30~`

과제 피드백.       

유효성체크.         

날짜검색, 상호관계.    

#

어제 이론 수업 이어서...    

## 리엑트(React) 수업

### # <리액트 문제9> 프로젝트명 > src > naver > Test.js 를 만들고, http://localhost:3000/naver/test 라고 입력하여 이 자원을 실행하여 열려면? 

```
■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■
<리액트 문제9> 프로젝트명 > src > naver > Test.js 를 만들고,
               http://localhost:3000/naver/test 라고 입력하여 이 자원을 실행하여 열려면? 
■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■

    안된다. 
    아래와 같은 절차를 밟아야한다.
    수업진행 상 일단 아래 처럼 한다.  

    -------------------------------------------------------------
    <1> VSCode 에서 react-router-dom 이라는 모듈을 다운 받는다.
    -------------------------------------------------------------
        콘솔 창에서 아래 처럼 입력 후 엔터를 친다.
        -----------------------------------------------
        C:\zzz\prj-react-01\yarn add react-router-dom
        -----------------------------------------------
    -------------------------------------------------------------
    <2> 프로젝트명 > src > index.js 파일에서 설치된 react-router-dom 이라는 모듈을 수입한다.
    -------------------------------------------------------------
        프로젝트명 > src > index.js 파일을 열고 상단에 아래 코딩을 삽입한다.
        ---------------------------------------------------------
        import { BrowserRouter } from 'react-router-dom';
        ---------------------------------------------------------
    -------------------------------------------------------------
    <3> 프로젝트명 > src > index.js 파일에서 아래 부분을 수정한다.
    -------------------------------------------------------------
        -----------------------
        <React.StrictMode>
            <App />
        </React.StrictMode>
        -----------------------
            ⇩
        -----------------------
        <BrowserRouter>
            <App />
        </BrowserRouter>
        -----------------------  
    -----------------------------------------------------------------
    <4> 프로젝트명 > src > App.js 파일에서 아래 처럼 수정한다.
    -----------------------------------------------------------------
        ----------------------------------------------
        프로젝트명 > src > App.js
        ----------------------------------------------
        import React, {Component} from 'react';
        import { Route } from 'react-router-dom';
        import Test from './naver/Test';

        const App = () => {
            return (
                <div>
                    <Route path="/naver/test" component={Test}/>
                </div>
            )
        }
        ----------------------------------------------

    -----------------------------------------------------------------
    <5> 프로젝트명 > src > naver > Test.js 파일을 아래처럼 생성한다.
    -----------------------------------------------------------------
        ----------------------------------------------
        프로젝트명 > src > naver > Test.js
        ----------------------------------------------
        import React, { Component } from 'react';

        const Test = () => {
            return (
                <div>
                    &nbsp;&nbsp;&nbsp;새로 만들었습니다!
                    <br></br>
                    <br></br>
                    &nbsp;&nbsp;&nbsp;하하하하!
                </div>
            )
        }
        ----------------------------------------------
```
> 스프링 하듯이하면 안된다.  
> 추가 설정이 필요하다.  

src 폴더 내에 naver 폴더 만들고,   

그안에 `Test.js` 파일 만들고,   

그안에 코딩하기.   

```js
import React, { Component } from 'react';

	const Test = () => {
		return (
			<div>
				새로 만들었습니다!
			</div>
		)
	}

export default Test;
```
> 이렇게만 하면,   
> `http://localhost:3000/naver/Test` 이렇게 넣어도 열리지 않는다.  

> 추가적인 설정이 필요하다.        
> 새 터미널 열어서,    
> 외부 모듈 설치를 해야 한다.     
>  
> 외부모듈 설치 명령어,   
> `PS C:\Users\SungWoo\Desktop\prj-react-01> yarn add react-router-dom`   
> 외부모듈 설치하고 태그를 추가해야한다.  

> `index.js` 열고 코드 수정 추가,       
```js
import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';
import reportWebVitals from './reportWebVitals';
import { BrowserRouter } from 'react-router-dom';


ReactDOM.render(
  // <React.StrictMode>
    <BrowserRouter>
    <App />
    </BrowserRouter>,
  // </React.StrictMode>,
  document.getElementById('root')
);
```


> `App.js` 에서,  기존코드 삭제하고, 추가.   
```js
import logo from './logo.svg';
import './App.css';

import React, {Component} from 'react';
import { Route } from 'react-router-dom';
import Test from './naver/Test';

function App() {
  return (
    <div>
      <Route path="/naver/test" component={Test}></Route>
    </div>
    // 나머진 주석처리.
```
> ![App.js설정](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210924%201023_React_%EC%A0%91%EC%86%8D%EA%B2%BD%EB%A1%9C%EC%84%A4%EC%A0%95_.png)    

#

----
### `10:30~`

#

리엑트 서버 끄는 법.  

`PS C:\Users\SungWoo\Desktop\prj-react-01> Taskkill /IM node.exe /F`
> node.js 종료.  

#

----
### `11:30~`

### # <리액트 문제10> 자바에서는 [클래스]와 [인터페이스]라는 [단위 프로그램]이 있는데, React 에서 사용되는 [단위 프로그램] 종류는?

#

### # <리액트 문제11> 클래스 컴포넌트의 선언 형식은?  

#

- 자바스크립트 복습.(사용자 정의 객체)

  ```javascript
  var stu_no = 1;
  var stu_name = "홍길동";
  var age = 24;


  var student = {
      stu_no:1
      ,stu_name:"홍길동"
      ,stu_name:"홍길동"
      ,age:24
      ,hobby:["낚시", "코딩"]
      ,family:[
        {f_name:"저팔순",birth_year:1960,relation:"모"}
        ,{f_name:"사오정",birth_year:1958,relation:"부"}
        ,{f_name:"저팔계",birth_year:2003,relation:"형제"}
      ]
  }

  alert( 
    student.stu_no 
    +" "
    + student.stu_name 
    +" "
    + student.age 
    + student.hobby[0]   
    + student.hobby[1]   

    + sudent.family[0].f_name
  )

  ```
  > 일회성 사용자 정의 객체. `student` 만 던지면,  
  > 세가지 정보를 다 받을 수 있다.  
  > 다량의 데이터를 한곳에 담아서 보내기 위해서.  
  > 리액트는 이런 사용자 정의 객체를 많이 사용한다.   
  > `,stu_name:"홍길동"` 사용자 정의 객체 안에 똑같은 속성변수명이 나오면 마지막것이 나온다. 덮어 씌우는 개념.  

#

### # <리액트 문제12> 함수 컴포넌터의 선언 형식은? 

#

```javascript
var xxx = function(){ ~ }
```
> 자스에서는 익명함수도 변수안에 넣을 수 있다.  
> 넣어뒀다가 꺼내서 호출 가능.  

#

화살표함수.  

함수 컴포넌트안에서만 나올 수 있는 `useState`

#

----
### `12:30~`

### # <리액트 문제13> 리액트에서 사용되는 자바스크립트에서 변수 선언 시 왼쪽에 붙이는 var, let, const 의 의미는?  (입)입사시험 면접. 아주 기본적인 내용.     

```
■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■
<리액트 문제13> 리액트에서 사용되는 자바스크립트에서 변수 선언 시 왼쪽에 붙이는 var, let, const 의 의미는?  
■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■
    ****************************************************************************
    종류                사용 가능 영역              초기화 이후 갱신 가능 여부
    ****************************************************************************
    var                  함수 내부                              가능
    ----------------------------------------------------------------------------
    let                  블록({~}) 내부                         가능 
    ----------------------------------------------------------------------------
    const                블록({~}) 내부                         불가능
    ----------------------------------------------------------------------------
```
> 기본적인 구분을 하여야 한다.  

#

### # <리액트 문제14> 리액트의 안에서 사용되는 자바스크립트에서 함수 형태를 정리한다면? 
> 화살표함수.  

```
■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■
<리액트 문제14> 리액트의 안에서 사용되는 자바스크립트에서 함수 형태를 정리한다면? 
■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■
    -----------------------------------------------------------------------
    일반함수
    -----------------------------------------------------------------------
        function 함수명( [매개변수명] ){
            실행구문;
            [return 리턴값;]    
        }
    -----------------------------------------------------------------------
    클래스 컴포넌트 내부에 선언된 메소드 모양 함수 
    -----------------------------------------------------------------------
        함수명( [매개변수명] ){
            실행구문;
            [return 리턴값;]    
        }
    -----------------------------------------------------------------------
    변수 안의 익명함수
    -----------------------------------------------------------------------
        변수명 = function( [매개변수명] ){
            실행구문;
            [return 리턴값;]    
        }
    -----------------------------------------------------------------------
    변수 안의 화살표 함수 형식1 (매개변수 0개이상, 실행구문 있고 리턴값이 0~1개 이상인 경우)
    -----------------------------------------------------------------------
        변수명 = ( [매개변수명] ) => {
            실행구문;
            [return 리턴값;]    
        }
    -----------------------------------------------------------------------
    변수 안의 화살표 함수 형식2 (매개변수 1개, 실행구문 있고 리턴값이 0~1개 이상인 경우)
    -----------------------------------------------------------------------
        변수명 = 매개변수명 => {
            실행구문;
            [return 리턴값;]    
        }    
    -----------------------------------------------------------------------
    변수 안의 화살표 함수 형식3 (매개변수 0개 이상, 실행구문 없고, 리턴값만 있을 경우)
    -----------------------------------------------------------------------
        변수명 = ( [매개변수명] ) => 객체아닌리턴값;
        변수명 = ( [매개변수명] ) => (객체리턴값);
    -----------------------------------------------------------------------
    변수 안의 화살표 함수 형식4 (매개변수 1개, 실행구문 없고, 리턴값만 있을 경우)  
    -----------------------------------------------------------------------
        변수명 = 매개변수명 => 객체아닌리턴값;
        변수명 = 매개변수명 => (객체리턴값);

    -----------------------------------------------------------------------
    <주의> 화살표 함수 안에서 등장하는 this.속성변수
    -----------------------------------------------------------------------


    -----------------------------------------------------------------------
    <주의> 화살표 함수가 아닌 함수 안에서 등장하는 this.속성변수
    -----------------------------------------------------------------------
        this.속성변수 내포하는 바로 위 객체의 속성변수를 가르킨다. 
```
> 화살표 함수에서, 블럭이 없으면, 객체메위주아닌값(실수,문자,boolean값)이 있다면, 이 값은 리턴이 없어도 무조건 리턴이 된다.  
> 중괄호가 없으면, 그 데이터가 리턴된다.  
> 그 값이 메위주면 `()`로 감싸주어야 한다.  

> 매개변수를 끌어안는 괄호도 없는 경우,  
> 매개변수명이 하나 나오고,  화살표 오른쪽에 값을 리턴하는경우는 둥근 괄호 생략 가능.  

- 화살표 함수와 화살표 아닌함수의 차이점?   

  ```js
  function Xxx(){
    this.name = "흰둥이"
    return{
      name:'검둥이'
      ,bark:function(){
        alert( this.name ); // 검둥이 출력
      }
    }
  }
  ```
  > 익명함수
  > 함수의 모양을 띠고 있으나,  
  > 객체화 가능.  
  > 클래스 함수라고도 함.  
  > alert 안의 this 는 익명함수 안에서 this.name 은 '검둥이'를 가리키는 것이다.  

  ```js
  function Xxx(){
    this.name = "흰둥이"
    return{
      name:'검둥이'
      ,bark:()=>{
        alert( this.name ); // 흰둥이 출력
      }
    }
  }
  ```
  > bark 오른쪽에 `=>` 화살표함수가 나오면,  
  > this.name 은 최종 바깥쪽의 '흰둥이'를 지칭한다.  


  ```js
  function twice(value){
      return value*2;
  }

  // 위 아래는 같은값이 계산되어 리턴되는 함수들이다.  

  const twice = (value) => value*2;
  ```
  > 화살표 함수로 간단하게 표현 할 수 있다.  
  > 블럭이 없으면 그 데이터를 바로 리턴한다는 뜻이다.  

#

----
### `13:20~`

  - 점심시간.

---
---

## # 오후수업

---
### `14:30~`

### # <리액트 문제15> 리액트에서 사용되는 자바스크립트에서 [Array 객체]와 [사용정 객체]의 독특한 복사 방법은?  

```
■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■
<리액트 문제15> 리액트에서 사용되는 자바스크립트에서 [Array 객체]와 [사용정 객체]의 독특한 복사 방법은?
■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■

    [전개 연산자]를 사용하면 Array 객체나 사용정 객체 안의 데이터를 쉽게 복사할 수 있다.

    **********************************************************
    [Array 객체] 안의 데이터를 다른 [Array 객체]로 복사하기
    **********************************************************
        ---------------------
        일반 자스 문법 사용
        ---------------------
            ------------------------------------------------------
            var movieList1 = [ '샹치', '듄', 'D.P' ];   
            var movieList2 = [ movieList1[0],movieList1[1],movieList1[2] ];
            ------------------------------------------------------
            var movieList1 = [ '샹치', '듄', 'D.P' ];   
            var movieList2 = movieList1.slice(0,movieList1.length);
            ------------------------------------------------------
        ---------------------
        전개 연산자 사용
        ---------------------
            var movieList1 = [ '샹치', '듄', 'D.P' ];   
            var movieList2 = [ ...movieList1 ];       // '.' 개수는 배열번수의 개수와는 상관없다.  

    **********************************************************
    [Array 객체] 안의 데이터를 하나씩 꺼내서 [일반 변수]로 복사하기 
    **********************************************************
        ---------------------
        일반 자스 문법 사용
        ---------------------
            var movieList = [ '샹치', '듄', 'D.P' ];   
            var movie1 = movieList1[0];
            var movie2 = movieList1[1];
            var movie3 = movieList1[2];

        ---------------------
        전개 연산자 사용
        ---------------------
            var movieList = [ '샹치', '듄', 'D.P' ];   
            var [movie1,movie2,movie3] = movieList;

    **********************************************************
    [사용정 객체] 안의 데이터를 다른 [사용정 객체]로 복사하기
    **********************************************************
        ---------------------
        일반 자스 문법 사용
        ---------------------
            var student = {sno:1, sname:'사오정', jumin_num:'001225-1018333'};
            var student2 = {sno:student.sno, sname:student.sname, jumin_num:student.jumin_num};
        ---------------------
        전개 연산자 사용
        ---------------------
            var student = {sno:1, sname:'사오정', jumin_num:'001225-1018333'};
            var student2 = {...student};

    **********************************************************
    [사용정 객체] 안의 데이터를 하나씩 꺼내서 [일반 변수]로 복사하기
    **********************************************************
        ---------------------
        일반 자스 문법 사용
        ---------------------
            var student = {sno:1, sname:'사오정', jumin_num:'001225-1018333'};
            var sno = student.sno;
            var sname = student.sname;
            var jumin_num = student.jumin_num;

        ---------------------
        전개 연산자 사용
        ---------------------
            var student = {sno:1, sname:'사오정', jumin_num:'001225-1018333'};
            var [sno,sname,jumin_num] = student;


    -------------------------------------------------------------------------
    <참고> 자바에서 "고양이", "강아지", "햄스터" 3개의 문자열을 가진 배열 객체 생성
    -------------------------------------------------------------------------
        --------------------------------------------------------------------
        String[] animal = new String[3];
        animal[0] = "고양이";
        animal[1] = "강아지";
        animal[2] = "햄스터";
        --------------------------------------------------------------------
        String[] animal = new String[]{"고양이", "강아지", "햄스터"};
        --------------------------------------------------------------------
        String[] animal = {"고양이", "강아지", "햄스터"};
        --------------------------------------------------------------------
            ------------------------------------------
            xxx 메소드 선언 (O)
            ------------------------------------------
            public String[] xxx(){
                String[] animal = new String[3];
                animal[0] = "고양이";
                animal[1] = "강아지";
                animal[2] = "햄스터";
                return animal;
            }
            ------------------------------------------
            xxx 메소드 선언 (O)
            ------------------------------------------
            public String[] xxx(){
                return new String[]{"고양이", "강아지", "햄스터"};
            }
            ------------------------------------------
            xxx 메소드 선언 (X)
            ------------------------------------------
            public String[] xxx(){
                return {"고양이", "강아지", "햄스터"};
            }
            ------------------------------------------
            xxx 메소드 선언 (O)
            ------------------------------------------
            public String[] xxx(){
                String[] animal = new String[]{"고양이", "강아지", "햄스터"};
                return {"고양이", "강아지", "햄스터"};
            }
            ------------------------------------------


    -------------------------------------------------------------------------
    <참고> 자스에서 "고양이", "강아지", "햄스터" 3개의 문자열을 가진 Array 객체 생성
    -------------------------------------------------------------------------
        ----------------------------
        var animal = new Array("고양이", "강아지", "햄스터");
        ----------------------------
        var animal = new Array();  // var animal = [];
        animal.push("고양이");
        animal.push("강아지");
        animal.push("햄스터");
        ----------------------------
        var animal = new Array();
        animal[0] = "고양이";
        animal[1] = "강아지";
        animal[2] = "햄스터";
        ----------------------------
        var animal = ["고양이", "강아지", "햄스터"];    
        ----------------------------

    *************************************************************************
    <참고> 언어별 비슷한 용도의 객체 표현   
    *************************************************************************
    =========================================================================
    자바의 배열 객체       => String[] animal = {"고양이", "강아지", "햄스터"};
    -------------------------------------------------------------------------
    자바의 ArrayList 객체  => ArrayList animal = new ArrayList(); 
                              animal.add("고양이");
                              animal.add("강아지");
                              animal.add("햄스터"); 
    -------------------------------------------------------------------------
    자스의 Array 객체      => var animal = ["고양이", "강아지", "햄스터"];    
    =========================================================================
    =========================================================================
    자바의 ? 객체          => HashMap student = new HashMap(); 
                              student.put("sno","1");
                              student.put("sname","사오정");
                              student.put("jumin_num","001225-1018333");
    -------------------------------------------------------------------------
    자스의 사용정 객체     => var student = {sno:1, sname:'사오정', jumin_num:'001225-1018333'};
    -------------------------------------------------------------------------
    자스의 JSON 객체       => var student = {"sno":1, "sname":'사오정', "jumin_num":'001225-1018333'};
    =========================================================================
```
> 자바에서 배열 객체를 생성할 때 중괄호를 쓴다.  
> 자스에서는 Array 객체가 배열과 컬렉션기능을 가진다.  

> 자바 자스 구분하기.  

> 자스에서 중괄호`{}`는 `사용자 정의 객체` 혹은 `JSON` 쓸 때 사용한다.  

> 자바의 배열은 웹개발에 잘 쓰이지 않는다.    

> 자스의 Array 는 push 로 추가가 가능하다.  

#

---
### `15:30~`

### # <리액트 문제16> 리액트에서 사용되는 [클래스 컴포넌트]와 [함수 컴포넌트]에서 JSX 문법을 간략 설명?

```
■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■
<리액트 문제16> 리액트에서 사용되는 [클래스 컴포넌트]와 [함수 컴포넌트]에서 JSX 문법을 간략 설명?
■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■

    ***************************************************************************
    JSX 문법은 아래 처럼 하나의 부모 태그로 열고 닫고 그 안에 JSX 문법이 삽입된다. 아니면 에러 발생한다.
    ***************************************************************************
        ---------------------------------(O)
        return(
            <HTML 태그명>
                JSX 문법 코딩
            </HTML 태그명>
        )
        ---------------------------------(O)
        return(
            <Fragment>
                JSX 문법 코딩
            </Fragment>
        )
        ---------------------------------(O)
        return(
            <>
                JSX 문법 코딩
            </>
        )
        ---------------------------------(X)
        return(
            <HTML 태그명>
                JSX 문법 코딩
            </HTML 태그명>
            <HTML 태그명>
                JSX 문법 코딩
            </HTML 태그명>
            
        )
        ---------------------------------(X)
        return(
            <Fragment>
                JSX 문법 코딩
            </Fragment>
            <Fragment>
                JSX 문법 코딩
            </Fragment>
        )
        ---------------------------------(X)
        return(
            <>
                JSX 문법 코딩
            </>
            <>
                JSX 문법 코딩
            </>
        )
        ---------------------------------(X)
        return(
            {/* 주석문 */}
            <HTML 태그명>
                JSX 문법 코딩
            </HTML 태그명>
        )
        ---------------------------------(O)
        return(
            <HTML 태그명>
                {/* 주석문 */}
                JSX 문법 코딩
            </HTML 태그명>
        )

    ***************************************************************************
    모든 태그는 여는 태그가 나오면 반드시 닫는 태그도 나와야 한다.
    단, 여는 태그만 있는 태그는 여는 태그 안에 마지막에 `/` 가 나와야 한다.    
    ***************************************************************************
        <div> ~ </div>                      => <div> 있으면 뒤에 </div> 가 있어야한다.
        <hr/>                               => <hr> `/` 가 없으면 에러.
        <input type="text" name="uname"/>   => <input type="text" name="uname"> `/` 가 없으면 에러

```
> 리턴안에 최상위 태그는 하나이어야 한다. 두개면 안됨.     
> 리턴안에 최상위 태그와 동급레벨 태그, 주석문 모두 안된다.  
> 무조건 최상위 태그 하나만 나와야 한다.     

#

----
### `16:30~`

전 시간 이어서,  

```
    ***************************************************************************
    JSX 문법은 [HTML 태그] 또는 [사용자정의 태그] 중심이고 [HTML 태그] 또는 [사용자정의 태그] 사이에  
    자바스크립트 데이터가 들어간다면 {자바스크립트데이터} 의 형태로 삽입된다.
    ***************************************************************************
        <button onClick={()=>{실행구문}}>
            +1
        </button>
    ***************************************************************************
    JSX 문법에서 [HTML 태그] 안에선 class 속성명은 사용할 수 없고 대신 className 속성명을 사용한다.
    ***************************************************************************
        -----------------------------------
        <div class="movies"> ~ </div>        => (X)
        -----------------------------------
        <div className="movies"> ~ </div>    => (O)
        -----------------------------------

    ***************************************************************************
    JSX 문법에서 주석문 형식은 아래와 같다.
    ***************************************************************************
        ------------------------------
        {/* 주 석 문 */}                => 태그 밖에 어디든 사용 가능. 단, return 바로 밑에는 삽입하지 말자.  
        ------------------------------
        //                              => 태그 내부에 한줄의     속성명=속성값       오른쪽에 삽입된다.
        ------------------------------
            <input 
                    type="text"
                    name="uname"   // DB 컬럼명과 일치함   => 주석 가능한 경우.  
            />  

            /* 주 석 문 */          => 에러는 아니지만 그대로 웹브라우저에 출력된다.   
            <div>홍길동</div>
    ***************************************************************************
    JSX 문법에서 [HTML 태그] 안에서 style 속성명의 속성값은 반드시 아래와 같은 형식으로만 준다.
    ***************************************************************************
        ------------------------------
        style={ 사용자 정의 객체 }
        style={ {xxx:데이터,yyy:데이터,~} }
        ------------------------------
            <주의> 리엑트에서 아래 처럼 쓰면 안된다.
                   style="background-color:lightblue; font-size:10pt,color:gray"

        ------------------------------
        HTML 에서 style 속성명의 속성값 주기 
        ------------------------------
            <div style="background-color:lightblue; font-size:10pt,color:gray">          => JSX 에서 이렇게 불가능.
                홍길동 
            </div>
        ------------------------------
        리액트에서 JSX 문법에서 style 속성명의 속성값 주기  
        ------------------------------
            --------------------------------------------------------------------------
            <div style={{backgroundColor:'lightblue', fontSize:'10pt',color:'gray'}}>    => 카멜 표기법으로 써야한다.
                홍길동 
            </div>
            --------------------------------------------------------------------------
            const xxx = {backgroundColor:'lightblue', fontSize:'10pt',color:'gray'};
            return(
                <div style={xxx}>
                    홍길동
                </div>
            )
```

----
### `17:30~`


리엑트 예제는 다음주부터.   

#

## # 스프링 코드 일부 변경.    

`LoginController.java` 에서,  

```java
  // ********************************************
  // 가상주소 /loginForm.do 로 접근하면 호출되는 메소드 선언
  // ********************************************
  @RequestMapping( value="/loginForm.do")
  public ModelAndView loginForm(
      
    ){
      System.out.print("==================================\r");  
      System.out.print("접속 성공, 로그인화면 진입했습니다..!\r");   
      System.out.print("==================================\r");   
      // ---------------------------
      // [ModelAndView 객체] 생성하기.
      // [ModelAndView 객체] 에 [호출 JSP 페이지명]을 저장하기
      // [ModelAndView 객체] 리턴하기.
      // ---------------------------
      ModelAndView mav = new ModelAndView();
      mav.setViewName(path+"loginForm.jsp"); // 경로에 "/WEB-INF/views/" 가 붙고 나머지가 붙는다.
      return mav;
  }
```
> 이부분 주석처리하고 복사하나 해놓고 수정.

```java
  @RequestMapping( value="/loginForm.do")
  public String loginForm( ){
      System.out.print("==================================\r");  
      System.out.print("접속 성공, 로그인화면 진입했습니다..!\r");   
      System.out.print("==================================\r");   

      return path+"loginForm.jsp";
  }
```
> 리턴형을 String 으로 바꾼다.  
> 호출할 페이지명을 바로 리턴하게 함.  

> DB 연동 필요없고 바로 jsp 페이지 이동할 때 사용하는 방법.

> `ModelAndView` 는 DB 연동 저장할 때 사용.  

#

----
### `18:30~`

이번주 과제,   

추석과제 추가 요구사항.     

직원등록 들어갔다가 혹은 수정하러갔다가 다시 나올때.    
이전페이지 그대로 나오게하기.    

힌트     
세션객체에 다 담거나,     
검색했던값을 다 들고갔다가 다시 오거나,   


검색화면서에서,     

![추석과제 검색조건 추가](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210924%201104_%EC%B6%94%EC%84%9D%EA%B3%BC%EC%A0%9C%EC%B6%94%EA%B0%80%EC%9A%94%EA%B5%AC%EC%82%AC%ED%95%AD_.png)     

1. 년 없이 월 선택시 선택 x  (년도 선택해주세요 경고창.)

2. 미니멈 년 선택 시, 월은 1월 선택.  

3. 맥시멈 년 선택 시, 월은 12월 선택.   

4. 미니멈 년 월 맥시멈 년 월이 모두 선택 됐을때,
    미니멈 년월이 맥시멈 년월보다 크면, 경고 띄우고 비워주어야 한다.  

5. 미니멈만 선택하면 그이상 전부가 되도록 검색되게 하기.  그냥 이상, 이하면 되도록.  

6. 년도를 비우면 월도 자동으로 비워주어야 한다.  

> 아무것도 모르는 유저가 사용할때 불편함이 없게 만들어야 한다.  


입력화면쪽 추가 요구사항.    

졸업일,   

년 월 일    

![졸업일 추가 요구사항](https://github.com/SungWoo0315/study-repository/blob/main/image-save/20210924%201110_%EC%B6%94%EC%84%9D%EA%B3%BC%EC%A0%9C%20%EC%A1%B8%EC%97%85%EC%9D%BC%20%EC%B6%94%EA%B0%80%20%EC%9A%94%EA%B5%AC%EC%82%AC%ED%95%AD.png)     

실제 2월의 마지막 날짜까지 나와야 한다.  

change 이벤트 발생할때(년월 두가지가 선택됐을때). 최대치의 일을 넣어야한다.  
실시간으로 옵션태그 삭제하고 새롭게 마지막날짜가  들어가야한다.  


그 달의 마지막 날짜.  

2020년 2월 마지막 날짜 구하는법.

```js
var d = new Date(2020, 2, 1-1)

var lastDay = d.getDate()
```
> 2 이지만 3월이다.  
> 3월 마지막 에서 전날을 구하면 2월 마지막 날짜가 된다.  

> 반복문 돌려서 옵션 태그 문자를 lastDay 까지 돌려서 덮어씌우기.  

#

다음주 화요일 28일 오전수업만,  
강사님 백신,  

#

10월 휴강일.   

4일,    
11일,    


#

마지막 화면 돌아가게 하는법 힌트. 

1. 폼태그를 만들어서 값을 다 집어넣고 매개변수로 searchDTO 를 가지고가서 다시 폼태그에 담아뒀다가 다시 들고 서치화면으로 가서 원화면이 나오도록 하게.

2. searchDTO 를 세션에 담아서 가는법.  

#

리엑트 끝나면 넥사크로 나갈 예정.  

#

추석과제 추가 요구사항.   

현재는.    
각 페이지 이동,    

대신에,   

검색화면 고정 상태에서 div 태그로 그 화면상태에서 나타났다가 입력 끝나면 사라지게 하기.    

